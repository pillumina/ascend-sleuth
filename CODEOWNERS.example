# CODEOWNERS.example —— owner 落实后复制为 .github/CODEOWNERS 并填入真实账号
#
# 作用：受保护分支上，命中路径的变更必须由对应 owner 批（配合分支保护 required review）。
# owner 未定前保持 TODO 占位——CI 与标签流程先跑通，机制先行、权限后紧。
# 详见 docs/git-workflow.md

# ---- 共享资产：高风险双签（两组各至少一人批）----
/triage-tree.yaml      @todo-domain-owner @todo-system-maintainer
/knowledge/common/     @todo-domain-owner @todo-system-maintainer

# ---- 方法论框架：体系维护人 ----
/skills/               @todo-system-maintainer
/scripts/              @todo-system-maintainer
/docs/                 @todo-system-maintainer
/eval/                 @todo-system-maintainer
/knowledge/platforms/  @todo-system-maintainer

# ---- 封存对照集：只有维护者能改（独立性的落点）----
# eval/holdout.yaml 钉住一部分 golden 夹具的内容哈希。它是"CI 绿是否有意义"的前提：
# 改动者能改被测对象、也能改量尺时，"无回归"不是独立信号。CI 已强制"改它须带
# holdout-change 标签"，但那只是半硬——**真正的独立性由本行 + 分支保护 required review
# 提供**。owner 落实前，请把 holdout 读成"标记可见"而非"已有人把关"（原则十）。
/eval/holdout.yaml     @todo-system-maintainer

# ---- 训练 namespace：训练 owner ----
/knowledge/training/   @todo-training-owner

# ---- 推理 namespace：推理 owner ----
/knowledge/inference/  @todo-inference-owner
