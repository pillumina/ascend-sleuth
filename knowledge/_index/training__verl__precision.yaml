# GENERATED FILE —— 分片（knowledge/_index.yaml 的 training/verl__precision 子集），不要手改。
# 由 scripts/build_index.py 生成；`--check` 校验新鲜度（groom/CI）。
# 阶段一加载协议（F1 EV-2026-022 + F4 EV-2026-025）：category 已定时 diagnose 只读命中 (namespace×category) 的类分片，不读整库总表。
# 本分片：training/verl__precision（1 条 case）
namespaces:
  training/verl__precision:
    precision:
    - id: VERL-LOGPROB-CONFIG-MISMATCH
      title: Kimi2.5 RL 训练 log_prob 首 step 指数差异过大：训练侧 YarnRotaryEmbedding 的 original_max_position_embeddings=1024
        与实际外层配置 4096 不一致，位置编码偏差致 MLA 输出不一致
      category: precision
      tags:
      - rl
      - train-infer-consistency
      - log-prob
      - rope
      - yarn
      - mla
      compat: verl *
      confidence:
        score: 0.7
      symptoms:
      - RL 训练中 log_prob 指数首 step 差异较大（超出预期范围）
      file: knowledge/training/verl/precision/VERL-LOGPROB-CONFIG-MISMATCH.yaml
      hash: 49220befe2f1
