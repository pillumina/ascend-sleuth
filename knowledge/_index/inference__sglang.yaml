# GENERATED FILE —— 分片（knowledge/_index.yaml 的 inference/sglang 子集），不要手改。
# 随 PR 提交（改 case 的人跑 `python3 scripts/build_index.py`）；
# 本目录配了 merge=union：两人同一天改同一格时，两边的条目都会留住，不产生冲突标记。
# 阶段一加载协议（F1 EV-2026-022）：category 未定 / 类分片缺失时 diagnose 回退读本 namespace 分片，不读整库总表。
# 本分片：inference/sglang
namespaces:
  inference/sglang:
    interrupt:
    - id: SGL-PD-HEAP-001
      title: PD 分离偶发失败：CombineMemories 合并 K+V 超过 heap_size 致 halShmemCreateHandle 拒绝
      category: interrupt
      tags:
      - pd-separation
      - kv-cache-transfer
      - sdma
      - combinememories
      - heap-overflow
      - non-deterministic
      compat: sglang <=0.5.10; memfabric_hybrid <=1.0.5
      confidence:
        score: 0.6
      symptoms:
      - PD 分离 --disaggregation-transfer-backend ascend，KV cache 跨 NPU 传输偶发失败
      sig: []
      tok: []
      file: knowledge/inference/sglang/SGL-PD-HEAP-001.yaml
      hash: 3576617fec9c
