# GENERATED FILE —— 分片（knowledge/_index.yaml 的 inference/sglang__interrupt 子集），不要手改。
# 人提交面：改 case 的人在**自己 PR 里提交本文件**（`build_index.py --check` 比的正是它）；
# 总表 knowledge/_index.yaml 不进 PR（合并后由合并者重建一次）。撞车时重跑生成器即可，不必手判留哪份。
# 阶段一加载协议（F1 EV-2026-022 + F4 EV-2026-025）：category 已定时 diagnose 只读命中 (namespace×category) 的类分片，不读整库总表。
# 本分片：inference/sglang__interrupt（1 条 case）
namespaces:
  inference/sglang__interrupt:
    interrupt:
    - id: SGL-PD-HEAP-001
      title: PD 分离偶发失败：CombineMemories 合并 K+V 超过 heap_size 致 halShmemCreateHandle 拒绝
      category: interrupt
      tags:
      - pd-separation
      - kv-cache-transfer
      - sdma
      - combinememories
      - heap-overflow
      - non-deterministic
      compat: sglang <=0.5.10; memfabric_hybrid <=1.0.5
      confidence:
        score: 0.6
      symptoms:
      - PD 分离 --disaggregation-transfer-backend ascend，KV cache 跨 NPU 传输偶发失败
      sig: []
      tok: []
      file: knowledge/inference/sglang/SGL-PD-HEAP-001.yaml
      hash: 3576617fec9c
