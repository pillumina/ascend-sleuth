# GENERATED FILE —— 背景类 summary 索引（diagnose 步骤 3 阶段 2.5 读取），不要手改。
# 由 scripts/build_ref_summary_index.py 生成；--check 校验新鲜度（CI）。
# 只含背景类 + status=active；查表类走步骤 2 收尾的键触发 grep（口径同 diagnose SKILL）。
# 读法：grep 取行 + ≤5 行上限——索引随词条数增长，整读等于注入全库背景。
# applies_to.categories 缺省 = 不限定问题类别（照常加载）；有值则按本轮 category 收窄。
# 生成日期：2026-09-21    词条数：133
entries:
- id: a5-950dt-memory-spec
  type: platform-fact
  title: A5 950DT 片上内存规格（144GB / 4TB/s）
  summary: 昇腾950DT 单芯片最高 144GB / 4TB/s 高速片上内存，覆盖大模型全生命周期
  applies_to:
    platforms:
    - A5-950DT
    categories:
    - interrupt
    - performance
- id: a5-950pr-memory-spec
  type: platform-fact
  title: A5 950PR 片上内存规格（128GB / 1.6TB/s）
  summary: 昇腾950PR 单芯片最高 128GB / 1.6TB/s 高速片上内存，面向推荐、LLM Prefill、多模态推理
  applies_to:
    platforms:
    - A5-950PR
    categories:
    - interrupt
    - performance
- id: a5-ai-subsystem-compute-spec
  type: platform-fact
  title: A5 AI 子系统数量与算力规格（Cube/Vector 数量 + TFLOPS 表）
  summary: 昇腾950 共 36 个 AI 子系统（各 1 Cube + 2 Vector）；Cube 数量 950PR 32/28、950DT 36/32/28；算力随精度格式分级
  applies_to:
    platforms:
    - A5-950
    categories:
    - performance
- id: a5-ccu
  type: platform-fact
  title: A5 CCU 集合通信计算加速单元
  summary: CCU 硬件卸载集合通信任务，计算通信深度并行，释放 AI Core 算力
  applies_to:
    platforms:
    - A5-950
    categories:
    - interrupt
    - performance
- id: a5-ccu-algorithms
  type: platform-fact
  title: A5 CCU 集合通信算法支持（Broadcast/ReduceScatter/AllGather/AllReduce/All2All/All2Allv）
  summary: CCU 硬件执行 6 种集合通信算法；CCUM Mission 接口 + CCUA（MemorySlice 存储 + Reduce Unit 计算）
  applies_to:
    platforms:
    - A5-950
    categories:
    - interrupt
    - performance
- id: a5-cube-core-spec
  type: platform-fact
  title: A5 Cube Core 规格（数值格式算力倍率、L0C Buffer、随路量化）
  summary: 第三代 Cube：FP8/MXFP8/HiF8 提供 2 倍 FP16 TFLOPS、MXFP4 提供 4 倍；更大 L0C Buffer；回写 UB 时随路量化（FP32→BF16/FP16/FP8、NZ→ND/DN）
  applies_to:
    platforms:
    - A5-950
    categories:
    - precision
    - performance
- id: a5-cv-fusion
  type: platform-fact
  title: A5 Cube-Vector 融合（CV 直连通道、FlashAttention 内部通路、随路转换）
  summary: Cube L1 Buffer 与 Vector Unified Buffer 直连通道；FA 带宽瓶颈的 Cube-Vector 内部数据通路；传输时随路精度/排布转换
  applies_to:
    platforms:
    - A5-950
    categories:
    - precision
    - performance
- id: a5-die-architecture
  type: platform-fact
  title: A5 多 Die 合封架构（2 AI Die + 2 IO Die + 片上内存模块）
  summary: 昇腾950 为 Chiplet 合封：2 AI Die + 2 IO Die + 8(PR)/4(DT) 片上内存模块，D2D Clink 互联，UMA 统一内存
  applies_to:
    platforms:
    - A5-950
    categories:
    - interrupt
    - performance
- id: a5-hif8-format
  type: platform-fact
  title: HiF8 精度格式细节（38 指数、无 MX 缩放因子、特殊值编码）
  summary: HiF8 综合阶码范围 [-22,15] 共 38 个指数，比 FP8 E4M3 的 18 个动态范围成倍提升；与 MXFP8 相比无需 8 位 MX 缩放因子
  applies_to:
    platforms:
    - A5-950
    categories:
    - precision
- id: a5-interconnect-ub2
  type: platform-fact
  title: A5 灵衢 Unified Bus 2.0 互联（2TB/s，URMA/UB Memory/PCIe 5.0/UBoE）
  summary: 昇腾950 单芯片互联带宽 2TB/s：72×HiLink(112Gbps)/18 Port，URMA 异步拷贝、UB Memory 同步语义、PCIe 5.0 x16 双模式、2×400Gbps
    UBoE
  applies_to:
    platforms:
    - A5-950
    categories:
    - interrupt
    - performance
- id: a5-l2-cache
  type: platform-fact
  title: A5 全局 L2 Cache（128MB，Chiplet UMA）
  summary: 昇腾950 提供 128MB 统一访问 L2 Cache，支持按 Way Cache Lock，管理粒度 128Byte Sector
  applies_to:
    platforms:
    - A5-950
    categories:
    - performance
- id: a5-l2-cache-detail
  type: platform-fact
  title: A5 L2 Cache 微架构与 Cache 管理（512B Cache Line、L2 hint、CMO）
  summary: A5 L2 Cache 512B Cache Line + 128B Sector，2 Die UMA 跨 Die 访问，硬件一致性，L2 hint 分配/替换策略，SDMA CMO（Prefetch/Writeback/Invalid/Flush）
  applies_to:
    platforms:
    - A5-950
    categories:
    - performance
- id: a5-memory-hierarchy
  type: platform-fact
  title: A5 Memory 层次与容量（表 4-2）
  summary: 昇腾950 Memory 层次：L1 512KB、L0A/L0B 64KB、L0C 256KB、Unified Buffer 512KB（per AI Core）；CPU L1 64KB/L2
    1MB/L3 4MB；L2 ≤128MB；片上内存 PR≤128GB、DT≤96/144GB
  applies_to:
    platforms:
    - A5-950
    categories:
    - interrupt
    - performance
- id: a5-memory-ras
  type: platform-fact
  title: A5 高速片上内存 RAS 特性
  summary: 昇腾950 片上内存支持 Online ECC、巡检、行失效动态隔离（用户无感）
  applies_to:
    platforms:
    - A5-950
    categories:
    - interrupt
- id: a5-nddma
  type: platform-fact
  title: A5 NDDMA 指令（多维数据排布转换、5 维重排、128B 读合并）
  summary: NDDMA 硬化地址生成，最多 5 维数据重排，128B 读操作合并，降低数据搬运编程复杂度
  applies_to:
    platforms:
    - A5-950
    categories:
    - performance
- id: a5-pcie5-spec
  type: platform-fact
  title: A5 PCIe 5.0 规格（x16 128GB/s、EP/RC 静态选择、DMA/MCTP）
  summary: 昇腾950 支持 PCIe GEN5 1x16/1x8/1x4/1x2，128GB/s 双向，EP/RC 双模静态选择，支持 DMA、MCTP 加速器
  applies_to:
    platforms:
    - A5-950
    categories:
    - interrupt
    - performance
- id: a5-precision-format-support
  type: platform-fact
  title: A5 精度格式支持（原生 MXFP4/MXFP8/HiF8，另含 FP8/INT8/TF32/FP16/BF16）
  summary: 昇腾950 系列原生支持 MXFP4/MXFP8/HiF8 低精度格式，全面支持 FP8/INT8/TF32/FP16/BF16
  applies_to:
    platforms:
    - A5-950
    categories:
    - precision
- id: a5-simd-simt-mixed
  type: platform-fact
  title: A5 SIMD/SIMT 混合编程架构（SIMD 为主、SIMT 为辅）
  summary: 昇腾950 DaVinci Core 采用 SIMD 为主 / SIMT 为辅的混合编程；SIMT 面向离散访问与复杂分支；Vector Function（VF）支持快速切换
  applies_to:
    platforms:
    - A5-950
    categories:
    - performance
- id: a5-stars2-scheduler
  type: platform-fact
  title: A5 STARS 2.0 任务调度器（HSCB、任务流、Group、资源池）
  summary: STARS 2.0 调度 AIC/AIV/CPU/DVPP/SDMA/UB/CCU；HSCB ns 级调度；2048 任务流下沉；8 Group 亲和性调度；16 资源池算力切分
  applies_to:
    platforms:
    - A5-950
    categories:
    - interrupt
    - performance
- id: a5-supernode-scale
  type: platform-fact
  title: A5 超节点规模（8K 卡）与集群规模（128K+ 卡）
  summary: 昇腾超节点从上一代 384 卡提升至 8K 卡，整体集群支持超 128K 卡，nD-Mesh/Clos 灵活组网
  applies_to:
    platforms:
    - A5-950
    categories:
    - interrupt
    - performance
- id: a5-ub-onchip-switch
  type: platform-fact
  title: A5 UB On Chip Switch（IO Die 内 9×x4 Port 转发，不占 DRAM 带宽）
  summary: 单 IO Die 内 9 个 x4 Port 间流量转发，经 NoC 完成，不进计算 Die、不占 DRAM 带宽
  applies_to:
    platforms:
    - A5-950
    categories:
    - performance
- id: a5-urma-semantics
  type: platform-fact
  title: A5 URMA 异步通信语义（操作集、RTP/CTP 传输模式）
  summary: URMA 异步语义：Write/Read/Send/Atomic（FetchAdd、CAS）；RTP 可靠传输（4 Port）vs CTP 简易传输（9 Port）；Doorbell
    + Jetty 队列 + UMMU 地址转换
  applies_to:
    platforms:
    - A5-950
    categories:
    - interrupt
    - performance
- id: a5-vector-core-spec
  type: platform-fact
  title: A5 Vector Core 规格（BF16 原生支持、RegFile、算力翻倍）
  summary: 第三代 Vector：单核 FP16/FP32 TFLOPS 较上代提升 100%；新增 BF16 原生支持与格式转换指令；UB 与 Vector ALU 间引入 RegFile
  applies_to:
    platforms:
    - A5-950
    categories:
    - precision
    - performance
- id: aclnn-quant-modes-and-sparsemode
  type: software-fact
  title: aclnn 量化模式与 sparseMode（T/C/K/G/B 五级与五种组合 / MX 的定义 / sparseMode 0-8 表与 varlen 限制）
  summary: 两块与『精度问题的成因判读』直接相关的语义。**① 量化模式（量化粒度）与 scale 的 shape**：pertensor（T）——左或右矩阵整张共用一个量化参数，左矩阵 (m,k)
    时 scale 形状 (1,)；perchannel（C）——只能作用于**右矩阵**、每个 channel 独立，右矩阵 (k,n…
  applies_to:
    platforms:
    - cross
    categories:
    - precision
- id: aclnn-two-phase-contract
  type: software-fact
  title: aclnn 两段式接口契约（一阶段 GetWorkspaceSize / 二阶段执行）
  summary: aclnn 单算子接口固定为两段：一阶段 `aclnnXxxGetWorkspaceSize` 做入参校验、动态 shape 推导、Tiling、构建 L0 计算图、算 workspace
    大小；二阶段 `aclnnXxx` 由 nnopbase 按图选算子提交到 NPU。据此把报错与耗时按段归属：一阶段失败多为入参 …
  applies_to:
    platforms:
    - cross
    categories:
    - interrupt
    - performance
- id: aclnn-type-promotion-broadcast-and-format
  type: software-fact
  title: aclnn 的张量语义约束（互推导/互转换/broadcast 的 <6 维限制/数据格式与维度要求/非连续 Tensor）
  summary: 写或排查 aclnn 调用时的四类张量语义约束。**① 互推导（type promotion，类比 PyTorch）**：输入 aclTensor 之间数据类型不一致时，API 内部推导出一个类型并把输入都转成它再算——规则是**整数类型间可推导、整数可往浮点与复数推、浮点间可推、浮点可往复数推、复数之间可推、BOOL…
  applies_to:
    platforms:
    - cross
    categories:
    - interrupt
    - precision
- id: aicore-task-duration-anatomy
  type: software-fact
  title: AICore 算子 Task Duration 分段与首指令取指链（Icache/TLB/Dcache）
  summary: profiling 里的单算子 Task Duration 由多段构成：AICore 调度器调度约 150ns、硬件初始化约 50ns、软件初始化约 1us、所有核执行结束到结果上报约
    500ns，来源明写**这个时间不是固定的**——由 TilingData 大小、算子入参个数、算子初始化行为决定（复杂算子的 Til…
  applies_to:
    platforms:
    - cross
    categories:
    - performance
- id: amct-quantization-precision-tuning
  type: software-fact
  title: AMCT 量化的精度三手段（基于精度的自动量化 / 校准集 batch_num 建议 16 或 32 / skip_layers 跳过损失大的层）与损失层两步比对法
  summary: 量化模型精度不够时的三条手段与一条定位法：①**基于精度的自动量化**——借助 AMCT 工具提供的 Python 接口，在保证用户所需模型精度前提下自动搜索模型的量化配置并执行**训练后量化**，生成满足精度要求的量化模型（当前
    PyTorch、TensorFlow、Caffe 框架支持）。②**校准集规模**——基…
  applies_to:
    platforms:
    - cross
    categories:
    - precision
- id: aoe-model-tuning
  type: tool
  title: AOE 模型调优（aoe 命令：先子图调优再算子调优 → 知识库/om/效果文件 → TUNE_BANK_PATH 指定后推理）
  summary: 推理性能调优的关键工具之一（另一个是模型压缩工具 AMCT）：`aoe` 命令在 atc 参数基础上增加 `--job_type`，提供**子图调优**与**算子调优**；**一般建议先做子图调优再做算子调优**——子图调优会生成图的切分方式，之后算子已被切成最终
    shape，再按最终 shape 做算子调优；顺序颠倒…
  applies_to:
    platforms:
    - cross
    categories:
    - performance
- id: asc-tools-ascendc-debug-suite
  type: tool
  title: Ascend C 算子调试工具集（cpu_debug 的 CPU 域编译 + npu_check 的 Error* 判据表 + msobjdump 的 ELF 字段 + show_kernel_debug_data
    + optype_collector）
  summary: asc-tools 这套工具解决算子级问题：**cpu_debug**——在 NPU 部署前先上 CPU 做功能与精度基本验证（bisheng 编译生成 CPU 域可执行程序，再用
    gdb 常规调试；编译选项 `CMAKE_ASC_RUN_MODE=cpu` 与 `CMAKE_ASC_ARCHITECTURES=dav…
  applies_to:
    platforms:
    - A2-910B
    - A3-910C
    - A5-950
    - cross
    categories:
    - interrupt
    - precision
    - performance
- id: ascend-310p-known-limits
  type: platform-fact
  title: 310P（Atlas 300I Duo）平台已知限制
  summary: 310P 平台特有限制：event 资源有限（cudagraph 捕获超限）、aicpu IndexPut 异常（驱动版本相关）、MTP 与图模式不可同时开启（缺 free-mask
    算子）
  applies_to:
    platforms:
    - 310P
    categories:
    - interrupt
- id: ascend-app-process-and-resource-constraints
  type: software-fact
  title: 应用侧进程与资源硬约束（单 Device 进程数上限 63/64、不支持 fork 后调 acl 接口、创建销毁配对、休眠前置）
  summary: 三类会直接表现为「进程起不来 / 报错 / 卡死」的应用侧硬约束：①**单 Device 用户进程数上限**（Host 上限 = Device 数 × 单卡上限）——Atlas 训练系列、Atlas
    推理系列、Atlas 200I/500 A2 推理产品、Ascend 950PR/950DT 为**单卡 64 个**（…
  applies_to:
    platforms:
    - A2-910B
    - A3-910C
    - A5-950
    - cross
    categories:
    - interrupt
    - performance
- id: ascend-asys
  type: tool
  title: asys 故障信息收集与解析工具（仅 Ascend EP 形态；含收集面/约束/五类解析/四类检测的完整口径）
  summary: asys 一键式收集故障信息并解析 plog 伴侣文件：collect/launch 收集（含业务复跑）、info 状态展示、health 健康检查、diagnose 压测检测（stress/hbm/cpu/aicore_stl/component）、analyze
    解析（trace/coredump/stackcor…
  applies_to:
    platforms:
    - cross
    categories:
    - interrupt
- id: ascend-device-log-ops
  type: tool
  title: Device 日志排查操作（RAS 故障 ID / 资源占用 / 进程拉起与结束时间）
  summary: 三组 Device 日志操作：①msnpureport -f 导出后 event_*.log 搜 event_id 取 RAS 故障码（npu-smi info -t health
    兜底 8 次）；②SYSMONITOR 看 fd usage（阈值 90%）与僵尸进程（阈值 5）；③slog/dev-os-id/[de…
  applies_to:
    platforms:
    - cross
    categories:
    - interrupt
- id: ascend-error-code-structure
  type: software-fact
  title: 昇腾 6 位错误码结构（级别位 + 模块位 + 内部码规则）
  summary: 错误码 6 位：第 1 位 E/W/I（错误/告警/提示）；第 2 位模块（E1=GE、E2=FE、E3=AI CPU、E4=TEFusion、E7=Vector 算子插件、E8=Vector
    算子、EB=TBE Pass、EC=Auto Tune、ED=RLTune、EE=Runtime、EF=LxFusion&Au…
  applies_to:
    platforms:
    - cross
    categories:
    - interrupt
    - precision
    - performance
- id: ascend-inference-multistream-and-graph-mode
  type: software-fact
  title: 推理侧多流并行与 NPU 图模式（两种并行的成立条件、三模式接口不可混用、Guards 命中是收益前提、9 条图模式高频问题）
  summary: 推理性能定位里最常被误解的两块机制。**多流**：Device 侧物理单元 Cube（大矩阵乘）、Vector（向量/激活/规约）、HCCL（集合通信）可独立调度，Stream 是
    Host 向 Device 下发任务的队列（同流串行、跨流由 Event 与调度器定序）；三种并行形态——**Cube↔Vector 互补…
  applies_to:
    platforms:
    - A2-910B
    - A3-910C
    - A5-950
    - cross
    categories:
    - performance
- id: ascend-log-classification
  type: software-fact
  title: 昇腾日志分类与统一格式
  summary: 昇腾日志分系统类（Device 侧）/ 应用类（Host+Device）/ trace（内存记录、出错或退出才落盘）三类；统一格式 [Level] ModuleName(PID,PName):DateTimeMS
    [FileName:LineNumber]LogContent；文件名中 id=Device ID、pid…
  applies_to:
    platforms:
    - cross
    categories:
    - interrupt
- id: ascend-log-levels
  type: tool
  title: 设置日志级别（应用类环境变量 / 系统类 msnpureport 或 slog.conf）
  summary: 日志级别序 0 DEBUG < 1 INFO < 2 WARNING < 3 ERROR < 4 NULL 不输出；调试日志默认 ERROR 可调、安全日志默认 DEBUG 不可调、运行日志默认
    INFO 不可调；应用类用 ASCEND_GLOBAL_LOG_LEVEL 等环境变量，系统类 EP 用 msnpurepo…
  applies_to:
    platforms:
    - cross
    categories:
    - interrupt
- id: ascend-log-locations
  type: software-fact
  title: 昇腾日志存储路径（EP / RC / Control CPU 开放形态，含 EP 现场采集包 npu_log_collect 布局）
  summary: 昇腾日志存储路径：EP 应用类 $HOME/ascend/log、系统类走 msnpureport 导出或现场采集包 npu_log_collect（driver_log/<时间戳>/slog/dev-os-id/，debug
    下有 device-id 与 device-os 两级）；event 文件名时间戳是创建时间…
  applies_to:
    platforms:
    - cross
    categories:
    - interrupt
- id: ascend-log-modules
  type: software-fact
  title: 昇腾日志模块名字典（ModuleName 解码）
  summary: 日志行 [Level] ModuleName(PID,PName):... 中 ModuleName 对照：Device 侧 SLOG/IDEDD/DVPP/TS/TSDUMP/LP/AICPU，Host
    侧 GE/ASCENDCL/DRV/RUNTIME/TBE/TDT/HCCL/TEFUSION/ATC/PLOG；…
  applies_to:
    platforms:
    - cross
    categories:
    - interrupt
- id: ascend-msaicerr
  type: tool
  title: msaicerr AI Core Error 专项分析工具（含功能约束、目录约束与四个子命令的参数口径）
  summary: msaicerr 分析 AI Core Error 问题（输出 info.txt 分析报告）、解析 Dump 文件（→.bin/.npy，含算子输入输出与 workspace）、转换
    Dump 数据类型（13 种 dtype）、检查环境（-e 跑内置算子样例）；不支持 MatmulAllReduce 类算子（Matmu…
  applies_to:
    platforms:
    - cross
    categories:
    - interrupt
- id: ascend-restart-slogd
  type: tool
  title: 重启 Device 侧日志进程 slogd
  summary: 修改日志配置或日志进程异常后重启 slogd：kill -15 <pid> 让其清理共享内存后自动拉起（反复失败才用 -9），ps -elf | grep log 确认；重启后系统类日志续写未满旧文件、应用类写全新文件；需
    Device 侧登录权限，EP 形态无此权限
  applies_to:
    platforms:
    - cross
    categories:
    - interrupt
- id: ascend-slog-conf
  type: tool
  title: Device 侧日志配置文件 /etc/slog.conf
  summary: RC 与 Control CPU 开放形态 Device 侧系统日志配置在 /etc/slog.conf：global_level、enableEvent、模块级别（5=跟随全局）、滚动规格、logAgentFileDir（≤255
    字节）、permission_for_all；改后必须重启 slogd 生效；Asce…
  applies_to:
    platforms:
    - cross
    categories:
    - interrupt
- id: ascend-stack-view
  type: tool
  title: 查看 Python / C&C++ 应用程序堆栈（py-spy / gdb）
  summary: 卡住进程取堆栈：Python 用 py-spy dump -p <pid> > pyspy.log（不中断进程）；C/C++ 用 gdb -p <pid> 后 bt 查看调用栈（需安装
    gdb）；进程中断/卡住定位的宿主侧配合手段
  applies_to:
    platforms:
    - cross
    categories:
    - interrupt
- id: ascend-trace-logs
  type: software-fact
  title: trace 日志机制与落盘路径（仅 Ascend EP 标准形态）
  summary: trace 将软件栈维测信息记内存、运行出错或进程结束时才落盘，仅 Ascend EP 标准形态支持；根目录 $HOME/ascend/atrace（ASCEND_WORK_PATH
    可重定向），事件三类 schedule/stackcore/exit；stackcore 与 *.bin 轨迹需 asys 工具解析，目…
  applies_to:
    platforms:
    - cross
    categories:
    - interrupt
- id: atb-dispatch-bound-and-runtime-optimizations
  type: software-fact
  title: ATB 算子下发链与 Host/Device Bound 判据（下发六步、Tiling 影响可达 10×、Tiling Cache 与 Setup 复用、Workspace 省 50%）
  summary: 判断推理性能瓶颈在 Host 还是 Device，以及 ATB 为此做什么。**两种瓶颈的判据**：Host Bound —— Host 下发较慢、Device 执行较快，**在 profiling
    图上表现为 Stream 上的 Kernel 之间存在空泡**，此时设备算力未被充分利用、应优化 Host 端下发；De…
  applies_to:
    platforms:
    - A2-910B
    - A3-910C
    - cross
    categories:
    - performance
- id: atc-precision-mode-and-switches
  type: software-fact
  title: ATC 精度模式与两个默认开启的优化开关（默认 fp16 / allow_fp32_to_fp16 / keep_dtype 例外清单 / 数据缓存优化 / 融合规则）
  summary: om 模型精度问题的三个配置面：①**精度模式**——模型转换不指定精度模式时**默认用 fp16** 计算；`--precision_mode=allow_fp32_to_fp16`
    表示算子支持 fp32 就用 fp32、不支持才用 fp16（高精度模式，提升精度但可能影响性能）；`--keep_dtype` 配一…
  applies_to:
    platforms:
    - cross
    categories:
    - precision
- id: attention-precision-chains
  type: software-fact
  title: Attention 两类精度损失链（FP8 per-block 量化的 P 压 fp8 / 衰减注意力的 exp 指数上溢）与验收判据
  summary: 量化与线性 attention 各有主要精度损失点：①FIA FP8 per-block 全量化里 **softmax 概率 P 被压到 fp8(E4M3)** 是最大损失点，且要求
    block_size 与算子 tiling 块一致（scale shape 直接暴露粒度：query 沿 S1 每 128、key/va…
  applies_to:
    platforms:
    - cross
    categories:
    - precision
- id: autofuse-fusion-enable-and-diagnostics
  type: tool
  title: AutoFuse 自动融合的使能与未融合原因判读（torch.compile 的 ascendc 后端 + autofused_ 产物 + Fallback 原因 + DFX 融合图）
  summary: AutoFuse 是面向 Ascend C 的自动融合框架（把相邻算子合成一个、消掉中间的输入输出搬运，缓解 Vector 链路的 Memory Bound），在 PyTorch 侧通过
    `torch.compile(model, dynamic=False, fullgraph=True, options={"npu…
  applies_to:
    platforms:
    - A2-910B
    - A3-910C
    - A5-950
    - cross
    categories:
    - performance
    - precision
- id: batch-consistency-and-determinism-switches
  type: software-fact
  title: 计算确定性开关（ACL_OPT_DETERMINISTIC 1 与 3 / torch 侧两个入口 / 逐比特一致与 batch 一致性的区别 / 版本门槛）
  summary: 昇腾上「结果不一致」分两级，开关也不同级。**① 确定性（determinism，`ACL_OPT_DETERMINISTIC=1`）**：解决同一算子在同一输入下多次运行结果不同——根源是算子实现内的异步多线程执行使浮点累加顺序变化；aclnn
    路径用 `aclrtSetSysParamOpt`（进程级）设 1，Py…
  applies_to:
    platforms:
    - cross
    categories:
    - precision
- id: bf16-intermediate-precision
  type: software-fact
  title: BF16 中间计算的精度处理（升 FP32 计算 / 等价重结合会改变 BF16 舍入位置）
  summary: BF16 的尾数只有 8 位，两类精度事故有明确机制：①**中间结果在 BF16 上直接计算**会累积舍入误差（修法是 BF16 load → FP32 compute → BF16
    store，来源实例最大偏差 0.0078125）；②**代数等价的重新结合**（如把 `(a+b)+c` 改成 `a+(b+c)`、或…
  applies_to:
    platforms:
    - cross
    categories:
    - precision
- id: cannsim-sim-profiling
  type: tool
  title: cannsim 仿真观测（SoC 级指令流水 cycle Trace + bit 级精度仿真，非上板采集；单卡/仅 AI Core 计算算子/仅 950PR）
  summary: cannsim 在 SoC 级仿真器上跑同一个 kernel（与上板二进制兼容），输出两类结果：**性能面**——cycle 级指令流水 Trace 与 VectorFunction（VF）的
    load / exec / store pipe 分布；**精度面**——bit 级精度结果用于算子精度验证。它补的是上板 p…
  applies_to:
    platforms:
    - A5-950
    categories:
    - performance
- id: crosscore-vs-mutex
  type: software-fact
  title: CrossCore 与核内 Mutex 的分工（跨核交接 vs 核内槽复用，不可互替、方向不对称）
  summary: 1C2V Mix 核里有**两套互不替代**的同步机制：CrossCore 只传递「数据就绪」与「槽位可复用」两类状态、**不搬运数值**，负责 AIC/AIV 共享资源的所有权；核内
    Mutex 负责单个核心内部物理槽的复用（分别约束 AIV 的 MTE2→Vector→MTE3、AIC 的 MTE1→Mmad 与 …
  applies_to:
    platforms:
    - A5-950
    categories:
    - interrupt
    - performance
- id: cv-fusion-fixpipe-ub
  type: software-fact
  title: Cube-Vector 融合与 Fixpipe L0C→UB 直通（仅 Ascend 950；判据 aiv_mte2_time 趋零）
  summary: Cube 累加结果默认在 L0C，传统通路是 Fixpipe 写到 GM、再由 AIV 用 MTE2 搬进 UB；**Ascend 950（dav-3510）的 Fixpipe 支持
    L0C 直通 UB**，A2/A3 无此能力。融合算子（同一 launch 内 AIC 计算 + AIV 后处理）的收益判据有两条：`a…
  applies_to:
    platforms:
    - A5-950
    categories:
    - performance
    - interrupt
- id: elemwise-vs-reduce-bound
  type: software-fact
  title: elemwise 是 throughput-bound、归约是 latency-bound（判据：循环携带依赖）
  summary: 判断 Vector 算子该往哪优化，分水岭是**相邻迭代之间有没有循环携带依赖**：elemwise 每个 lane 只读写自己的位置、无依赖 → **throughput-bound**，基线已最优，手写展开或
    `#pragma unroll` 基本无效（发射已打满，只多出标量地址指令）；归约是多对一、累加器要用上一…
  applies_to:
    platforms:
    - A5-950
    categories:
    - performance
- id: exec-npu-cmd-async-launch
  type: software-fact
  title: EXEC_NPU_CMD 单算子下发（V1/V2 线程归属 + 十步异步提交 TaskQueue）
  summary: torch_npu 每个算子下发的固定展开：`task_queue_enable == 2` 走 `EXEC_NPU_CMD_V2`（参数预拷贝），否则走 V1（直接转换）；两者**只差一阶段
    `GetWorkspaceSize` 在哪个线程执行**——V1 在主线程、V2 在消费线程，二阶段 `aclnnXxx` 两…
  applies_to:
    platforms:
    - cross
    categories:
    - performance
    - interrupt
- id: fabricmem-unified-addressing-and-transfer
  type: software-fact
  title: FabricMem 模式（超节点内 DRAM 统一编址 + D2RH/RH2D 单边传输：VMM 四步、开关与取值约束、依赖与硬件范围）
  summary: HIXL 的 FabricMem 传输模式：在 Atlas 800T A3 超节点内把**所有计算节点的 DRAM 统一编址**，NPU 通过 HCCS 直接访问远程节点内存，从而走
    **D2RH（设备→远程主机）/RH2D（远程主机→设备）** 高带宽通道——文档给出的动机是传统 RoCE 方案满载带宽约 20GB/…
  applies_to:
    platforms:
    - A3-910C
    categories:
    - interrupt
    - performance
- id: flash-attention-stage-pipeline
  type: software-fact
  title: FlashAttention 四阶段的三条依赖与跨轮次错位（FIA_PRELOAD_TASK_CACHE_SIZE = 3）
  summary: FA 的 mm1/vec1/mm2/vec2 四阶段由 Cube 核与 Vector 核分担，依赖关系**只有三条**：①同一轮次的四阶段顺序执行；②不同轮次之间的 vec1 顺序执行；③不同轮次之间的
    vec2 顺序执行。因此**非同一轮次的 mm1 与 vec1/vec2、mm2 与 vec1/vec2 都可并行*…
  applies_to:
    platforms:
    - cross
    categories:
    - performance
- id: float8-cast-bit-mapping
  type: software-fact
  title: float4_e2m1 → float8_e4m3fn 无直接 Cast（位映射 + ×64 补偿，bias 需先 ÷64）
  summary: Ascend 950 不支持从 `float4_e2m1` 直接转 `float8_e4m3fn`：需走 `float4_e2m1 → bfloat16 → float32 → float8_e4m3fn`
    共 3 个 Cast。等效位操作是符号位不变、指数位高位补两个 0、尾数位低位补两个 0；该结果被解释为 e4m…
  applies_to:
    platforms:
    - A5-950
    categories:
    - precision
    - performance
- id: fractal-nz-layout
  type: platform-fact
  title: FRACTAL_NZ 分形排布（基本块 16×16 / FP32 16×8，K 对齐 32/sizeof(T)，N 对齐 16）
  summary: NPU 内部计算亲和 **FRACTAL_NZ** 分形排布：基本块 FP16/BF16 为 16×16、FP32 为 16×8（M 维块恒 16，FP32 时 N 维减半）；块间按
    N 字形（列优先）、块内按 Z 字形（Morton）排列，ND 则是行主序。对齐规则：K 维对齐到 `CUBE_SIZE/sizeof(…
  applies_to:
    platforms:
    - A5-950
    categories:
    - precision
    - performance
    - interrupt
- id: full-load-l1-condition
  type: software-fact
  title: 全载（FullLoad）把小矩阵常驻 L1 的判定条件与后果
  summary: MTE2 Bound 场景下，若 A（或 B）矩阵能完整放进 L1，可一次性载入并常驻复用，把 MTE2 搬运次数从「与计算轮次成正比」降到**常数级**。判定条件三条：①分块数需超过可用核数（`tilesM=(m+255)/256`、`tilesN=(n+255)/256`，要求
    `tilesM×tilesN ≥ n…
  applies_to:
    platforms:
    - A5-950
    categories:
    - performance
    - precision
- id: hccl-cluster-heartbeat-and-link-probe
  type: software-fact
  title: HCCL 集群心跳维测链路与建链故障探测链路（事件类型、判据窗口、日志格式）
  summary: HCCL 在两个阶段各自建了一条「自动指根节点」的旁路机制：①**集群心跳**——基于通信域信息与邻近 rank 建独立维测链路，把单点故障广播到通信域内每个 rank（HCCL 控制链路数量与数据量，不影响通信性能），探测三类事件：网络问题
    ERROR CQE/Error cqe Occurred（轮询 ROCE 驱…
  applies_to:
    platforms:
    - cross
    categories:
    - interrupt
- id: hccl-log-key-info-fields
  type: software-fact
  title: HCCL 关键日志的检索关键字与字段含义（Communicator/LocalRank Key Info、HCCL_ENV、Entry- 算子入参、task exception 三段）
  summary: 从 CANN 日志里取 HCCL 证据的四个检索入口：①`Communicator Key Info` / `LocalRank Key Info`——一行的通信域关键信息（identifier/rankSize/serverNum/moduleNum/superPodNum
    及两个「是否不一致」标志）与本端关键信息（…
  applies_to:
    platforms:
    - cross
    categories:
    - interrupt
    - performance
    - precision
- id: hccn-tool-network-diagnosis
  type: tool
  title: hccn_tool（网口链路闪断 / device IP / ROCE 与 HCCS 连通性 / TLS 状态与证书）
  summary: 用 hccn_tool 检查 device 侧网络链路健康与 TLS 一致性的一套命令。**旧版写法**（模式开关在尾）：`-link_stat -g` 查网口 up/down 次数与闪断时间点（ERROR
    CQE 的常见根因）、`-ip -g`/`-vnic -g`/`-ip -inet6 -g` 查 NIC/VNI…
  applies_to:
    platforms:
    - cross
    categories:
    - interrupt
    - performance
- id: hif8-and-mxfp8-low-precision-training
  type: software-fact
  title: HiF8 / MXFP8 低精度训练（格式特性、Per-Tensor vs Per-Block、Delayed vs Current Scaling、950DT 实测 29.5% 加速与参数组合支持范围）
  summary: 训练低精度（8-bit）的选型与判据。**格式**：HiF8 由符号 S(1) + 点位 Dot(2~4) + 阶码 E(D 位，**符号-幅度编码**且幅值最高位为隐含位) + 尾数
    M(1~3) 组成；Normal 模式下 Dot=1/2/3/4 分别编码 E=±1、±[2,3]…共覆盖指数 [-15,15] 且*…
  applies_to:
    platforms:
    - A5-950
    categories:
    - precision
    - performance
- id: hif8-type-contract
  type: software-fact
  title: HiFloat8 的类型契约（hifloat8_t 建 tensor / Cast 直转 / 按 1 Byte 搬运）
  summary: 算子开发侧不需要感知 HiFloat8 的转换计算逻辑：把输入/输出数据类型设为 `hifloat8_t` 即可，支持创建该类型的 GM 与 UB（`GlobalTensor<hifloat8_t>`
    / `LocalTensor<hifloat8_t>`）；类型转换直接用 `AscendC::Cast<hifloat…
  applies_to:
    platforms:
    - A5-950
    categories:
    - precision
    - performance
- id: hixl-tool-route-and-comm-res
  type: tool
  title: hixl_tool（A5/950 通信资源生成：host_route 导 EID 清单 + local_comm_res 生成 LocalCommRes JSON）
  summary: HIXL 附带的命令行工具，为分布式推理/AI 场景生成网络通信资源，**仅适用于 A5（Ascend 950）机型，不支持 A2/A3**。两个子命令：`host_route` 导出本机
    Host/CPU 侧 EID（2 口 PG）与 NPU 侧 EID 到 `host_route.json`（默认 `/etc/`，…
  applies_to:
    platforms:
    - A5-950
    categories:
    - interrupt
    - performance
- id: host-kernel-log-collection
  type: tool
  title: Host 内核日志采集与判读（dmesg / journalctl -k / /var/log/messages）
  summary: Host 侧内核日志三条入口（dmesg -T 与时间窗、journalctl -k 与 -b -1、/var/log/messages）+ 判读面（OOM killer 的 Out
    of memory / Memory cgroup out of memory 消息格式、重启与链路事件入口）+ 四个坑（环形缓冲被覆盖…
  applies_to:
    platforms:
    - cross
    categories:
    - interrupt
- id: hw-task-scheduler-routing
  type: platform-fact
  title: 硬件任务调度模块分派（AICore / AICpu / Sdma 三条调度通道）
  summary: 昇腾芯片的系统任务调度模块把任务按类型分派到不同调度器：硬件队列 → 路由请求 → {AICore 调度器, AICpu 调度器, ……, Sdma 调度器} → {AIC, AIV,
    AICpu, ……, Sdma}。即 AIC/AIV 任务走 AICore 调度器，而 AICpu 任务与 Sdma 拷贝任务**各有…
  applies_to:
    platforms:
    - cross
    categories:
    - interrupt
    - performance
- id: l1-l0-bank-and-conflict
  type: platform-fact
  title: L1/L0 片上存储的 Bank 结构与冲突（L1 双 256KiB Bank、UB bank 交织公式）
  summary: 片上存储的内部划分决定了「搬运不快」的一类根因：L1 为 512 KiB、按 256 KiB 分成两个 Bank，跨 Bank 的访问模式会打断 MMAD；L0C 固定 256 KiB，直接决定
    `baseM = baseN = 256` 是 tiling 上限；UB 侧存在 bank 交织（`bank = (byte…
  applies_to:
    platforms:
    - A5-950
    categories:
    - performance
- id: l2-and-multibuffer-mechanisms
  type: software-fact
  title: matmul 的 L2 与多缓冲机制（SWAT 提命中率 / 只读权重绕过 L2 / K 尾轮 L0C 冲突 / 2-3-4 Buffer 条件）
  summary: 四个与 L2、多缓冲相关的机制：①**SWAT**（M 向固定窗口 + N 向 Z 型往复滑动）通过提高多核单次访问的 **L2 命中率**改善 MTE2，做到首轮搬运即可 MMAD
    不断流，Cube 利用率可达 95%+；②**只读一次的 weight 可绕过 L2**（双页表），不适用的数据不必占 L2；③**K …
  applies_to:
    platforms:
    - A5-950
    categories:
    - performance
- id: llm-datadist-link-and-kv-transfer-contract
  type: software-fact
  title: LLM-DataDist 建链与 KV Cache 传输契约（类 TCP 建链 / rets 逐个判 / Pull 与 Push 的层范围语义 / 角色切换前置）
  summary: PD 分离里 LLM-DataDist 的传输面契约。**① 建链是类 TCP 流程**：Server 侧初始化时提供侦听信息（`OPTION_LISTEN_IP_INFO = ip:port`），Client
    侧调 `LinkLlmClusters` 发起；**Server/Client 与 prompt/decod…
  applies_to:
    platforms:
    - A2-910B
    - A3-910C
    - cross
    categories:
    - interrupt
    - precision
- id: mc2-megamoe-token-capacity
  type: software-fact
  title: MC2 / MegaMoe 的 token 容量与 dispatch_ffn_combine 的 workspace 构成（含 mega_moe_max_tokens 默认值）
  summary: '**① MC2 token 容量是算出来的，不是一个固定值**：`set_mc2_tokens_capacity()` 按三分支取 `max_num_tokens`——开了 `enable_prefill_mc2`
    取 `max_num_batched_tokens`；否则若有 cudagraph 捕获尺寸取 `max…'
  applies_to:
    platforms:
    - cross
    categories:
    - interrupt
    - performance
- id: moe-dispatch-combine
  type: software-fact
  title: MoE Dispatch/Combine 的对等内存单边写机制（mte_put / K+1 状态位 / 通信前量化收益边界）
  summary: EP 扩大后 MoE 推理的主瓶颈是 Dispatch/Combine 通信。替代传统 `alltoallv` 的做法是：基于对称共享内存按 token 逐个发送/接收，用 `mte_put`
    跨 PE 单边写对端窗口偏移（`aclshmem_ptr(shmemCtx, pe) + offset`），并用 pipeli…
  applies_to:
    platforms:
    - cross
    categories:
    - interrupt
    - performance
- id: msdebug-operator-source-debug
  type: tool
  title: msdebug 算子源码级调试（lldb 定制 + stub 劫持在 AiCore 上打断点；需 root 开 /proc/debug_switch 与 DWARF）
  summary: MindStudio 的算子**源码级调试工具**（底层基于 lldb 定制）：通过注入 `libruntime_stub.so` 劫持库拦截运行时 `libruntime.so`
    里的 kernel 启动调用，从而在 **AiCore 上**（不是宿主机 CPU 上）对核函数源码设置断点，做源码级断点/单步/变量查看…
  applies_to:
    platforms:
    - cross
    categories:
    - interrupt
    - precision
- id: msopprof
  type: tool
  title: msopprof 算子级性能采集（流水图 + 带宽类 CSV 指标，与 TTK 配合须关 Profiling 与预热）
  summary: MindStudio 的**算子级性能采集与分析工具**：采集算子执行过程中的**硬件流水、带宽利用率**等细粒度数据，生成流水图 `visualize_data.bin`（供 MindStudio
    Insight 查看）与 `L2Cache.csv`、`MemoryL0.csv` 等性能数据文件。**与 TTK 配合…
  applies_to:
    platforms:
    - cross
    categories:
    - performance
- id: msprobe-accuracy-checker
  type: tool
  title: msprobe acc_check——离线精度预检（API 达标判定链路与阈值）
  summary: msprobe acc_check：在 NPU 上扫描训练模型的 API 输入数据，与 CPU 高精度标杆逐 API 比对给出达标判定；判链是「余弦相似度 > 0.99 → 最大绝对误差
    < 0.001 → 双百/双千/双万」；输出 accuracy_checking_result/details_{ts}.csv；m…
  applies_to:
    platforms:
    - cross
    categories:
    - precision
- id: msprobe-accuracy-compare
  type: tool
  title: msprobe compare——数值精度比对（NPU vs 标杆，真实数据/统计量/MD5 三模式）
  summary: msprobe compare：把 NPU dump 与 CPU/GPU/NPU 标杆 dump 逐 API 比对，输出 compare_result_{ts}.csv（多卡按 rank
    分文件）；三种数据模式（真实数据/统计数据/MD5）对应三套表头与指标；加 -da 可自动识别全网首个差异节点
  applies_to:
    platforms:
    - cross
    categories:
    - precision
- id: msprobe-capability-limits
  type: software-fact
  title: msprobe 五功能的适用场景与当前版本局限性（选型与边界）
  summary: msprobe 选型与能力边界：dump（API 级仅支持白名单、对 inplace 支持有限、不支持参数及参数梯度，且**加工具可能让同步问题消失**）/ 离线预检（不受累计误差影响，但依赖
    GPU 环境、不支持通信算子、仅部分融合算子）/ 整网比对与可视化（可疑算子受累计误差影响、大模型耗时长）/ 训练状态监测（仅…
  applies_to:
    platforms:
    - cross
    categories:
    - precision
- id: msprobe-checkpoint-compare
  type: tool
  title: msprobe ckpt 比对——逐参数 l2/余弦相似度（Megatron/MindSpeed）
  summary: msprobe ckpt 比对：`msprobe config_check --compare <ckpt1> <ckpt2>` 逐参数输出 l2（欧氏距离）/cos（余弦相似度）/numel/shape
    到 json；支持 Megatron-LM 与 MindSpeed（含 TP/PP/EP/VPP 与 megatr…
  applies_to:
    platforms:
    - cross
    categories:
    - precision
- id: msprobe-compile-precision-checker
  type: tool
  title: msprobe PrecisionChecker——eager 与 compile 逐模块精度比对（single-pass / two-pass）
  summary: msprobe PrecisionChecker：给启用 torch.compile 后出现的 loss 抖动/收敛变差/输出不一致定位差异来源——wrap 标记模块后分别以 eager
    与 compile 执行，逐模块比对前向输出与输入梯度；训练场景用 single-pass（install+collect），推理/…
  applies_to:
    platforms:
    - cross
    categories:
    - precision
- id: msprobe-config-check
  type: tool
  title: msprobe config_check——训练前配置检查（双环境差异比对）
  summary: msprobe config_check：对比两个训练环境下影响精度的配置差异（环境变量/第三方库版本/训练超参/权重/数据集/随机操作）；静态采集只覆盖前三项，动态采集才覆盖全部；比对输出
    result.xlsx，summary 页六项 pass/error/warning，前五项必须在精度比对前 pass
  applies_to:
    platforms:
    - cross
    categories:
    - precision
- id: msprobe-data-dump
  type: tool
  title: msprobe dump 精度数据采集（config.json 配置 + PrecisionDebugger 接口）
  summary: msprobe dump：PyTorch 精度数据采集——config.json 配 task/dump_path/rank/step/level/scope/list/summary_mode，代码里用
    PrecisionDebugger(config_path).start/stop/step 采集；输出 dump…
  applies_to:
    platforms:
    - cross
    categories:
    - precision
- id: msprobe-data-load
  type: tool
  title: msprobe load 精度数据覆盖加载（隔离前序累计误差）
  summary: msprobe load：用已采集的模块级（L0）输入 tensor 覆盖目标模块的实际输入，隔离前序模块累计误差、精准定位本模块及下游问题；config.json 顶层 load
    段配 path/modules/step/rank/dump_after_load；要求源 dump 与目标模型模块结构、命名严格一致
  applies_to:
    platforms:
    - cross
    categories:
    - precision
- id: msprobe-monitor
  type: tool
  title: msprobe monitor——训练状态监测（v1 全功能 / v2 轻量 CSV）
  summary: msprobe monitor：训练中采集权重/权重梯度/激活值/优化器状态/通信算子统计——v1（TrainerMon，README 主指向、能力最全：异常告警/堆栈/l2 特征/mbs
    粒度/csv2tensorboard）与 v2（TrainerMonitorV2，轻量 CSV 子集，按 rank 落 rank_…
  applies_to:
    platforms:
    - cross
    categories:
    - precision
    - performance
- id: msprobe-overflow-check
  type: tool
  title: msprobe overflow_check——整网首个 NaN/Inf 节点定位（多卡）
  summary: msprobe overflow_check：对 PyTorch dump 数据定位多卡场景下每张卡首个产生 inf/nan 的节点（经通信传播的可回溯到首个异常 rank）；输出
    anomaly_analyze_{ts}.json 或打印无异常；判定口径是「输入正常而输出异常」才算溢出起点，输入已异常者是传播受害者
  applies_to:
    platforms:
    - cross
    categories:
    - precision
- id: msprobe-response-anomaly
  type: tool
  title: msprobe response_anomaly——推理异常检测（生僻字/乱码/重复/NaN logprobs）
  summary: msprobe response_anomaly：基于输出 token + topk logprobs 的**无参照**实时异常检测，返回 [is_ill, ill_type]（1=生僻字
    2=乱码 3=重复 4=NaN Value）；库接口 analyze_output_anomaly(topk_logprobs, …
  applies_to:
    platforms:
    - cross
    categories:
    - precision
- id: msprobe-trend-visualization
  type: tool
  title: msprobe data2db 趋势可视化——统计量按 step/rank/张量三维趋势（含 Megatron 并行切分可视化）
  summary: msprobe 趋势可视化：`msprobe data2db --db <dir> --data <dump/monitor 目录>` 把 dump(L0/mix) 或 monitor(csv)
    解析成 .trend.db，再用 TensorBoard 的 TREND ANALYZER 从 step/rank/张量目标…
  applies_to:
    platforms:
    - cross
    categories:
    - precision
    - performance
- id: msprobe-visualization
  type: tool
  title: msprobe graph_visualize——分级可视化构图比对（TensorBoard 图/颜色判定）
  summary: msprobe graph_visualize：把 dump 数据还原成模型图结构，用 TensorBoard 做结构与精度双图比对；节点颜色 error>warning>pass>未匹配，判定规则按真实数据/统计数据/MD5
    三模式分列；支持溢出筛选、名称搜索、跨套件比对与多卡构图
  applies_to:
    platforms:
    - cross
    categories:
    - precision
- id: msprof-cli-collect-items
  type: software-fact
  title: msprof 命令行采集项开关与频率语义（task-time l0-l3、aic-mode/aic-freq、sys-* 系列范围与默认值、host-sys 依赖）
  summary: msprof 命令行采集项的取值与默认值：`--task-time` 支持 l0/l1/l2/l3（l3 为 PyPTO 算子试用特性）；`--ai-core` 默认随 `--task-time`
    联动（on/l1→on，off/l0→off）；`--aic-mode` 取 task-based（**采集 AI 任务性…
  applies_to:
    platforms:
    - A2-910B
    - A3-910C
    - A5-950
    - cross
    categories:
    - performance
- id: msprof-collect-parse
  type: tool
  title: msprof 性能数据采集与解析（命令行两种传参方式 / --type / --storage-limit / 动态与延迟采集 / PROF_* 输出结构）
  summary: msprof 采集性能数据：**方式一（推荐）** `msprof python3 train.py --output=<dir>` 在命令末尾直接跟用户程序，**方式二** `msprof
    --application="python3 train.py"`（参数含需加引号的符号时建议写进 Shell 脚本再传）；采集…
  applies_to:
    platforms:
    - cross
    categories:
    - performance
- id: msprof-collection-mechanism-and-metric-semantics
  type: software-fact
  title: msprof 采集链路与指标口径（task-time 三级别与膨胀、task-based vs sample-based PMU、aicore_time 调频误差与 950 syscnt、l2
    开关的 postp 中断）
  summary: 读 msprof 数据前要知道的四件事：①**采集链路**——命令行/多种使能方式统一收敛为 `ProfileParams`，Host 侧由 runtime/GE/HCCL 回调上报（经无锁缓冲，上报与落盘解耦），AICPU
    侧由 Device 上的 aicpu 进程与集合通信进程上报，Device 侧经 `prof_…
  applies_to:
    platforms:
    - A2-910B
    - A3-910C
    - A5-950
    - cross
    categories:
    - performance
- id: msprof-configuration-options
  type: software-fact
  title: msprof / PROFILING_OPTIONS 配置项语义与平台差异（task_time 级别、aic_metrics 与 Custom 寄存器、l2、各 freq 范围、host_sys
    依赖）
  summary: 配置采集项的语义与取值：**默认**——PROFILING_MODE=true 且未配 PROFILING_OPTIONS 时默认采 training_trace、task_trace、hccl、aicpu
    与 aic_metrics(PipeUtilization)，数据存在 AI 任务所在目录。**关键开关**：`…
  applies_to:
    platforms:
    - A2-910B
    - A3-910C
    - A5-950
    - cross
    categories:
    - performance
- id: msprof-enablement-methods
  type: software-fact
  title: 性能数据使能方式与 mstx 打点（msprof 命令行 / acl C&C++ 接口 / acl.json / Ascend Graph / 环境变量 + 依赖工具安装）
  summary: 采性能数据的五种使能方式与各自入口：①**msprof 命令行**（拉起业务采集，最常用）；②**acl C&C++ 接口**（应用内调用采集与落盘、msproftx 扩展接口、订阅算子信息，采集数据另有说明）；③**acl.json
    配置文件**（字段与 PROFILING_OPTIONS 同构：switch/out…
  applies_to:
    platforms:
    - cross
    categories:
    - performance
- id: msprof-output-reading-and-performance-analysis
  type: software-fact
  title: msprof 默认产物清单与性能分析路径（timeline 三区域 / 按 Total Time 与 Task Duration 排序 / prof_rule 调优建议 / fusion_op
    融合前后）
  summary: 拿到一次 msprof 采集结果后怎么读：**默认配置产出的 9 类文件**——`msprof_*.json`（timeline 总表）、`step_trace_*.json`（迭代轨迹，每轮迭代耗时；单算子场景无）、`op_summary_*.csv`（AI
    Core 与 AI CPU 算子数据）、`op_stati…
  applies_to:
    platforms:
    - cross
    categories:
    - performance
- id: msprof-timeline-lane-reading
  type: tool
  title: msprof 硬件流水时间线泳道读法（泳道名格式 / 指令名 / 时间轴三段式）
  summary: 读 MindStudio 硬件流水时间线（Profiling 导出）的方法：每个 pipe 一条泳道，泳道名格式为 `0X.<PIPE>.<NN> [<kernel名>_<shape>_<子块>]
    <core号>`，同一 pipe 会按子块/核拆成多条泳道；泳道上的指令块可直接读出指令名（`MOV_OUT_TO_L1`…
  applies_to:
    platforms:
    - A5-950
    - cross
    categories:
    - performance
- id: mssanitizer
  type: tool
  title: mssanitizer 算子级异常检测（memcheck / racecheck / initcheck / synccheck 四子工具，可包裹 TTK 执行）
  summary: MindStudio Sanitizer：算子级异常检测工具，四个子工具——**memcheck**（默认；非法读写、多核踩踏、非对齐访问、内存泄漏、非法释放、分配内存未使用）、**racecheck**（数据竞争
    WAW/WAR/RAW）、**initcheck**（读取未初始化内存导致的脏数据）、**syncche…
  applies_to:
    platforms:
    - cross
    categories:
    - interrupt
    - precision
- id: mte2-preload-queue-depth
  type: platform-fact
  title: MTE2_PONG 指令队列深度阻塞（双缓冲下搬运流水断流）
  summary: 启用双缓冲的矩阵乘里，每条 `MTE2_PONG` 必须等配对的 `MTE2_PING` 之后**所有指令发射完毕**才有发射机会；两条指令之间插入的指令数一旦超过芯片预设指令队列深度，即使二者无同步依赖，PONG
    也发不出去，表现为 MTE2 流水断流。这与「搬运量大」是两回事——靠缩小 tiling 块治不好；解法…
  applies_to:
    platforms:
    - A5-950
    categories:
    - performance
- id: mx-quantized-matmul
  type: software-fact
  title: MX 量化矩阵乘约束（Cube 每拍 16×C0×16 / baseK 必须 64 倍数 / MXFP4 按 B8 搬 / scale L1 缓存 3 份）
  summary: MX（microscaling）量化的 matmul 有四条硬约束：①Cube 每拍计算量是 `16 × C0 × 16`（MXFP8 的 C0=32、MXFP4 的 C0=64），算力下界据此估算；②MMAD
    要求 `baseK` 是 **64 的整数倍**，K 非对齐要补零且补零方式随排布而异；③MXFP4 的 `…
  applies_to:
    platforms:
    - cross
    categories:
    - precision
    - interrupt
    - performance
- id: n-buffer-multi-level-pingpong
  type: software-fact
  title: N-Buffer 多级 ping/pong（L1 / L0A-L0B / L0C 各级独立配缓冲+事件标志）
  summary: 双缓冲及以上不是只在某一级加一份缓冲，而是 L1、L0A/L0B、L0C **各级各自配一对 ping/pong**，各级用独立缓冲区索引（`l1BufId = l1PingPong
    & 1`、`l0BufId = l0PingPong & 1`）与**独立事件标志**循环切换，才使「搬运-计算」在每一级重叠。反过来说…
  applies_to:
    platforms:
    - A5-950
    categories:
    - performance
- id: npu-arch-token-map
  type: platform-fact
  title: 架构标识 ↔ 平台映射（dav-3510 = Ascend 950 / dav-2201 = Ascend 910B,C）
  summary: CANN 工具链用架构标识指定目标 NPU：`dav-3510` = Ascend 950（A5）、`dav-2201` = Ascend 910B/C（A2/A3）。该标识出现在构建配置（`NPU_ARCH`）与编译器参数（`bisheng
    --npu-arch=dav-3510`）中——判断一份报错、一个二进制或一…
  applies_to:
    platforms:
    - A5-950
    - A2-910B
    - A3-910C
    categories:
    - interrupt
    - performance
- id: npu-prefetch-and-superkernel
  type: software-fact
  title: NPU 权重预取（npu_prefetch 的四个前提、size 估算、npugraph_ex 需显式预取流）与 SuperKernel（二进制融合的四类算子级优化与五条约束）
  summary: 两项 Decode 性能优化的机制与判据。**Prefetch**：Decode 小 batch 下 MatMul/QBMM/GMM 常是 memory-bound（耗时为读权重主导，weight
    常驻 HBM 数十 MB、激活很小），`torch_npu.npu_prefetch(weight, depend, si…
  applies_to:
    platforms:
    - A2-910B
    - A3-910C
    - A5-950
    - cross
    categories:
    - performance
- id: npu-smi-950-device-ops
  type: tool
  title: npu-smi（Ascend 950 版）设备查询与维护（info -t 全族 / watch 实时监控 / ECC 与告警 / TLS 证书 / 持久化模式日志转储）
  summary: 950 平台 npu-smi 的查询与维护命令面。**查询**：`npu-smi info -l` 列全部设备（拿逻辑 ID）、`npu-smi info` 基本信息、`npu-smi
    info -m` 设备映射、`npu-smi info -t <topic> -i <dev_id>` 按主题查——`board`（N…
  applies_to:
    platforms:
    - A5-950
    categories:
    - interrupt
    - performance
- id: om-dynamic-shape-and-aipp-constraints
  type: software-fact
  title: 动态 Shape 输入与 AIPP 的组合约束（四种设置方式只能选一、AIPP 与动态维度互斥、内存按最大档申请、size=0 即动态）
  summary: 动态 Shape 推理（动态 Batch / 动态分辨率 / 动态维度 / Shape 范围）与 AIPP 组合时的硬约束：①**同一模型四种动态设置方式只能选一种**——aclmdlSetDatasetTensorDesc
    设 Shape 范围、aclmdlSetDynamicBatchSize 设动态 Batch、…
  applies_to:
    platforms:
    - cross
    categories:
    - interrupt
    - precision
    - performance
- id: op-compiler-static-kernel
  type: tool
  title: op_compiler 静态 Kernel 编译（dump 算子信息 → 编译 run 包 → 安装 → 匹配性校验 → 卸载）
  summary: 用静态 Kernel 提升算子执行性能的工具链：**原理**——静态 Kernel 编译在编译时指定算子 shape、运行时不再指定 shape，算子编译工具按算子信息统计文件得到确定
    shape 并为每个 shape 各编译一份算子二进制，从而提升执行效率（编译期已知 tensor 大小故存储利用率高、可针对实际 s…
  applies_to:
    platforms:
    - A2-910B
    - A5-950
    - cross
    categories:
    - performance
- id: operator-debug-entrypoints
  type: tool
  title: 算子级调试与调优入口（plog 路径 / aclGetRecentErrMsg / AI Core printf·DumpTensor / AI CPU KERNEL_LOG / msprof
    op / cannsim record）
  summary: 算子执行失败或精度异常时，按『先拿 Host 侧日志与框架错误信息 → 再往 Kernel 里加打印 → 复杂场景升级到单步调试』的顺序取信息。**① Host 侧**：plog 默认落在
    `$HOME/ascend/log/debug/plog/plog-pid_*.log`；`ASCEND_SLOG_PRINT_T…
  applies_to:
    platforms:
    - A2-910B
    - A3-910C
    - A5-950
    - cross
    categories:
    - interrupt
    - precision
    - performance
- id: pcie-host-memory-access
  type: software-fact
  title: PCIe Through 判定（Host 内存直算强制 SIMD Tiling）与三条件检查
  summary: PCIe Through 是零拷贝通路：Device 可直接访问 Host 内存（或反向），省掉显式 `Memcpy`。判定需**三个条件同时满足**——`OP_PCIE_THROUGH_ACCESS_HOST_MEM_CHECK_ENABLE=1`
    开启特性、`aclrtGetDeviceInfo(..., ACL_…
  applies_to:
    platforms:
    - cross
    categories:
    - performance
    - interrupt
- id: perf-modeling-constants
  type: platform-fact
  title: 性能建模参考常数（1.65GHz / 1µs=1650 cycle / MTE1 256BPC / L2 5.2TB/s / L2 命中率 28.9% 门槛）
  summary: 把 profiler 的 µs 换算成 cycle 与有效带宽、再与建模值比对的参考常数集：32 核、**1.65GHz**（故芯片上 **1 µs = 1650 cycle**）、HBM
    1.6TB/s、L2 5.2TB/s、MTE1 **256BPC**（≈422.4GB/s）、L0C 256KiB；MTE2 综合…
  applies_to:
    platforms:
    - A5-950
    categories:
    - performance
- id: regbase-execution-model
  type: software-fact
  title: RegBase 执行模型与收益判据（UB 往返 1.72x / IPC 上限 2 / --cce-res-usage 读寄存器与 Stack）
  summary: RegBase（`__simd_vf__` + `RegTensor`）与 MemBase（`LocalTensor` + 标准 Vector API）的差别只在 **UB 内计算方式**：MemBase
    一步计算就是「UB 读 → 算 → 写回 UB」，中间结果反复落 UB 时 `RVECLD + RVECST` 可…
  applies_to:
    platforms:
    - A5-950
    categories:
    - performance
    - precision
- id: runtime-first-setdevice-latency
  type: software-fact
  title: 首次 aclrtSetDevice 秒级时延的构成（算子包搬移 + 每进程独立 aicpu_scheduler + 并发竞争）
  summary: 业务进程**首次**调用 aclrtSetDevice 有明显秒级时延、后续调用降到毫秒级，成因有三条且性质不同：①**算子包搬移**——首次调用时 Runtime 把业务所需算子包从
    Host 侧传输并部署到 Device 侧，耗时与算子包大小正相关；搬移成功后结果缓存在 Device 侧，**版本一致的后续进程不重…
  applies_to:
    platforms:
    - cross
    categories:
    - performance
- id: runtime-memory-allocation-strategies
  type: software-fact
  title: Runtime 内存分配策略与页类型（4K/2M/1G、≤1M 时 HUGE_FIRST 退化普通页、P2P 后缀策略、IPC 页表对齐）
  summary: 选 aclrtMalloc 策略时的四条硬事实：①页类型——普通页 4K 适合小块/短生命周期/数量多的申请（减少对齐浪费），2M 大页适合大块/长期驻留/频繁访问（减少页表项、扩大
    TLB 覆盖），1G 大页适合超大块、长期驻留、性能敏感场景（部分产品支持）；②策略语义——`ACL_MEM_MALLOC_HUGE_F…
  applies_to:
    platforms:
    - cross
    categories:
    - performance
    - interrupt
- id: runtime-plog-failure-task-fields
  type: software-fact
  title: 从 plog 提取失败 task / 内核 / Device 异常的检索关键字与字段含义（Task run failed、fault kernel_name、The error from
    device）
  summary: Device 侧任务失败的证据链在 plog 里按五个关键字逐段取出：`Task run failed`（失败任务：device_id/stream_id/task_id/sqe_type/errType/sqSwStatus）、`fault
    kernel_name`（失败内核名 + `_high_performanc…
  applies_to:
    platforms:
    - cross
    categories:
    - interrupt
    - precision
- id: runtime-stream-event-device-semantics
  type: software-fact
  title: Runtime 的 Stream / Event / Device 语义（Device 归属、默认 Device 与默认 Stream 的创建销毁时机、同步范围与传参规则）
  summary: 三条决定定位方向的语义：①**Device 归属**——Stream 与 Event 在创建时绑定 Device，只能在该 Device 上使用，Event 只能关联到与其同 Device
    的 Stream；跨 Device 协调要用 Event 同步，不能跨 Device 复用 Stream（否则 aclrtMemc…
  applies_to:
    platforms:
    - cross
    categories:
    - interrupt
    - performance
    - precision
- id: scatter-write-conflict
  type: software-fact
  title: Scatter 并发写同址的覆盖语义（last-writer-wins，结果随调度顺序变化）
  summary: '`Scatter` 的并发写是**覆盖语义**（last-writer-wins），不是可交换归约——多个 lane/线程命中同一目标地址时，最终值取决于执行顺序，**同输入多次运行结果可以不同**。修法是把写操作按
    (目标地址, 组内位置) 排序后**只让组尾写**，把不确定的并发覆盖变成确定的单写'
  applies_to:
    platforms:
    - A5-950
    categories:
    - precision
- id: shmem-usage-limits-and-troubleshooting
  type: software-fact
  title: SHMEM 使用限制与常见问题（对称内存分配、UB Buffer 预留、A2 16 卡跨组禁 MTE、端口桥、QP 建链超时、卡间同步须在 AIV）
  summary: SHMEM（多机多卡内存通信库）的七条使用限制与常见问题的判据。**限制**：①GM2GM 高阶 RMA 用默认 buffer **不支持并发**（会覆盖数据，需并发就用低阶接口）；②`barrier`
    当前**必须在 Mix Kernel（含 mmad 与 GM2UB/UB2GM 操作）中使用**；③RDMA 高阶接…
  applies_to:
    platforms:
    - A2-910B
    - A3-910C
    - A5-950
    - cross
    categories:
    - interrupt
    - precision
    - performance
- id: simd-vf-programming-constraints
  type: software-fact
  title: SIMD Vector Function 约束集（入参白名单 / 硬循环退化 / Aux Scalar 仅整数 / UnalignReg ≤4）
  summary: 自定义算子（Ascend C SIMD Vector Function）的一族硬约束，多数以**编译失败**或**静默退化**表现：入参类型有白名单、入参数量受 PUSH_PB 头开销限制、循环要能被识别成硬件循环否则退化为
    software loop（控制流指令暴涨）、VF 内标量计算只允许 16/32 位整数（禁浮…
  applies_to:
    platforms:
    - A5-950
    categories:
    - interrupt
    - performance
- id: simt-execution-model
  type: platform-fact
  title: SIMT 执行模型与硬件上限（Warp 32 / Block ≤2048 / 一个 AIV 同时只跑一个 Block）
  summary: SIMT 线程层次为 Thread / Block / NumBlock 三层：Warp 固定 32 线程、一个 Block 的多个 Warp 依次调度到同一个 AIV；单 Block
    最多 **2048 线程**（硬件当前上限），且**一次在一个 AIV 上只能运行一个 Block**（与 GPU 不同），NumBl…
  applies_to:
    platforms:
    - A5-950
    categories:
    - performance
    - interrupt
- id: torch-npu-op-routing
  type: software-fact
  title: torch_npu 算子路由（PrivateUse1 注册 / redispatch / 兼容实现开关 / aclnn-aclop 分支）
  summary: PyTorch 张量在 NPU 上的算子调用要过三层路由：①Dispatcher 从参数提取 keyset（Autograd+PrivateUse1）并取最高优先级，Autograd
    优先于 PrivateUse1，故先命中 VariableType；②redispatch 用 `ks & after_autograd…
  applies_to:
    platforms:
    - cross
    categories:
    - interrupt
    - performance
    - precision
- id: torchtitan-npu-compile-backends-and-limits
  type: software-fact
  title: torchtitan-npu 的图编译契约（两级 backend 必须在 import torch 前定 / 按 Block 全图编译 / fallback 与动态 shape 边界 /
    内核缓存与「训练成功≠融合生效」）
  summary: 在 torchtitan-npu 上做 torch.compile 的性能与中断定位，先记住五条。**① 两级 backend 不能互相替代**：`compile.backend=inductor`
    选 PyTorch Dynamo backend，`TORCHINDUCTOR_NPU_BACKEND=ascendc`…
  applies_to:
    platforms:
    - A2-910B
    - A3-910C
    - cross
    categories:
    - performance
    - interrupt
    - precision
- id: torchtitan-npu-sdc-detection
  type: software-fact
  title: torchtitan-npu 的 SDC 检测（静默数据损坏：gradient / matmul checksum / HCCL 三路，typed 参数默认值与前置约束）
  summary: torchtitan-npu 内置的 **SDC（Silent Data Corruption，静默数据损坏）检测**，检测 torch.compile + Inductor 编译训练中的
    gradient、HCCL 与 matmul checksum 异常。**默认关闭**，通过 `TrainerEx.Config.…
  applies_to:
    platforms:
    - A2-910B
    - A3-910C
    - cross
    categories:
    - precision
    - performance
- id: torchtitan-npu-training-metrics-and-debugging
  type: tool
  title: torchtitan-npu 训练可观测面（Memory Snapshot 抓 OOM/泄漏 + NPU 适配 Profiling 的周期/绝对步数采集与离线解析）
  summary: torchtitan-npu 的两条观测路径，都通过**分层 CLI 参数**（默认全关）启用。**① Memory Snapshot**（查 OOM 与内存泄漏）：`--profiler.enable-memory-snapshot`
    定时采集（`--profiler.profile-freq` 默认 10 步一次，…
  applies_to:
    platforms:
    - A2-910B
    - A3-910C
    - cross
    categories:
    - performance
    - interrupt
- id: transfer-granularity-thresholds
  type: platform-fact
  title: 搬运粒度阈值（单次总量 <20K 不友好、连续 <128B 浪费 Sector-Cache）
  summary: 两个搬运粒度阈值：单次搬运总量小于 **20K** 对芯片搬运不友好、速度下降；Ascend 950 支持 **128B Sector-Cache**，但为了用满核把连续搬运字节数降到
    128B 以下会浪费有效带宽。可操作判据是 `N ≤ coreNum × 128` 的场景要检查任务是否被切得过碎（128 为 N 方…
  applies_to:
    platforms:
    - A5-950
    categories:
    - performance
- id: ttk-deterministic-levels-and-batch-consistency
  type: software-fact
  title: TTK 确定性等级 0–3 与 batch 一致性判据（batch relation 三字段齐全才进第二阶段）
  summary: TTK 用 `--deterministic-level`（缩写 `--dl`）控制一次执行的确定性等级：**0** 不启用确定性选项（只做普通精度校验）；**1** 同一用例的确定性执行；**2**
    编译期与执行期确定性选项（通路原有的强确定性检查）；**3** 确定性执行，**且只有在「完整声明 batch rel…
  applies_to:
    platforms:
    - cross
    categories:
    - precision
- id: ttk-dump-and-performance-testing
  type: software-fact
  title: TTK 的 Dump 调试与性能测试口径（--dump/--dump-on-fail、warmup×run 冷热两模式、四条通路的采集差异）
  summary: 两件在算子级定位里最常用的事：①**Dump 调试**——`--dump in|out|golden|full`（可组合，逗号分隔）分别在「输入生成后·设备执行前」「设备执行后·精度比对前」「Golden
    生成后」保存张量；`--dump-format` 取 bin（默认、无 shape）/npy（推荐，numpy.l…
  applies_to:
    platforms:
    - cross
    categories:
    - precision
    - performance
- id: ttk-precision-comparison-methods
  type: software-fact
  title: TTK 算子精度比对的 8 种方法与各 dtype 阈值（mixed/stat_rel_err/close/cosine/binary/requant/quant/cross_check）
  summary: 判「算子精度是否达标」时的判据表：默认方法 **mixed（混合容差）** 逐元素判 `|a-g| <= atol + rtol*|g|` 且要求通过率 ≥ **0.99**，另有绝对误差硬上限
    `max_abs_error_limit`（与 `32*ULP` 取满足任一）拦灾难性离群点；dtype 阈值——float…
  applies_to:
    platforms:
    - cross
    categories:
    - precision
- id: ub-capacity-and-budget
  type: platform-fact
  title: UB 容量口径（每 AIV 248 KiB 可用 / 每 AI Core 512 KiB）与 buffer 预算
  summary: UB 预算判据是 `queBytes × bufNum × 队列数 ≤ UB 可用容量`，其中 `queBytes` 是**每个 buffer** 的字节数、单条队列实占 `bufNum
    × queBytes`。dav-3510/Ascend 950 的可用值来自运行期查询：**每颗 AIV 独享 248 KiB 私有…
  applies_to:
    platforms:
    - A5-950
    categories:
    - performance
    - interrupt
- id: unit-flag-mmad-fixpipe
  type: software-fact
  title: unit_flag：按 512B 块解绑 MMAD 与 FixPipe（尾轮取值 3 / 非尾轮 2）
  summary: 当计算基本块过大占满 L0C、无法开缓存机制时，MMAD（计算）与 Fixpipe（搬出）无法并行；`unit_flag` 是指令级的解绑手段：MMAD 与 FixPipe 两侧同时配置后，计算单元每完成
    **512B** 数据块，FixPipe 立即搬出该块，不必等全部算完。MMAD 侧 `para.unitFlag…
  applies_to:
    platforms:
    - A5-950
    categories:
    - performance
    - precision
- id: verl-cross-validation
  type: tool
  title: verl 训推交叉打桩——Rollout Skip + 交叉验证（精度定界在训练还是推理）
  summary: verl 交叉验证：用打桩替换一个阶段的输出实现精度定界——两种模式 ckpt（推理执行并保存 rollout 数据 / 训练执行并加载 ckpt）与 rollout（推理加载 rollout
    数据跳过推理 / 训练执行并保存 ckpt），两个训练过程通过共享数据目录交换数据；另有 Rollout Skip 基础功能（…
  applies_to:
    platforms:
    - cross
    categories:
    - precision
- id: verl-official-image-no-hdk
  type: software-fact
  title: verl 官方昇腾 Docker 镜像不含 HDK
  summary: verl 官方昇腾镜像（quay.io/ascend/verl:*）不含 HDK，需按 install_guidance.rst 自选安装——HDK 版本与镜像组合不兼容是 verl+昇腾故障的常见根因面
  applies_to:
    platforms:
    - cross
    categories:
    - interrupt
- id: verl-probs-diff-monitoring
  type: tool
  title: verl 逐 token prob diff 监控——rollout 与 actor 前向的概率差
  summary: verl 逐 token prob diff 监控：逐 token 计算 rollout（vLLM）与 actor 前向（训练引擎）对同一 response 的对数概率差，输出到训练日志并落
    npy；前提必须设 actor_rollout_ref.rollout.calculate_log_probs=True；偏差来…
  applies_to:
    platforms:
    - cross
    categories:
    - precision
- id: verl-uwc-weight-consistency
  type: tool
  title: UWC 训推权重一致性验证——RL 训练侧到 vLLM 推理侧权重同步链路三段比对
  summary: UWC（Unified Weight Consistency）验证 RL 训推一体场景下「训练侧（verl+Megatron-core）→ 推理侧（vLLM）」权重同步链路的数值一致性，把链路拆成三段独立可比对：①Bridge
    转换 HF↔mcore ②跨进程传输 ③vLLM 加载；容差为 P0 vs P2 atol=…
  applies_to:
    platforms:
    - cross
    categories:
    - precision
- id: vf-call-and-barrier-rules
  type: software-fact
  title: asc_vf_call 的屏障语义与 UB 屏障规则（VF 期间 MTE2 被阻塞 / VF 内 LocalMemBar 会死锁）
  summary: 两条容易踩的 VF 调用规则：①`asc_vf_call` 是**屏障**——VF 执行期间 MTE2 被阻塞，预取的 `DataCopyPad` 也得等 VF 结束才能执行，且**屏障时间守恒**（3
    次短 VF 合并成 1 次长 VF 不减少总阻塞）；官方模式改用 `__no_simd_vf_fusion__` +…
  applies_to:
    platforms:
    - A5-950
    categories:
    - performance
    - interrupt
    - precision
- id: weight-nz-inference-only
  type: software-fact
  title: 权重预转 FRACTAL_NZ 只适用于推理（训练每步变权重无法预处理）
  summary: Matmul 以 ND 传入权重时需在数据通路中**随路做格式转换**，引入额外带宽损耗；把权重提前转成 FRACTAL_NZ 可消除这部分开销、提高带宽利用率——但**只适用于推理**：训练场景权重每步变化，无法预处理，随路转换的额外耗时消不掉。收益随权重矩阵规模增大而变高。诊断含义：不要把推理侧的
    NZ 优化建议套…
  applies_to:
    platforms:
    - A5-950
    categories:
    - performance
