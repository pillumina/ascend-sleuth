# GENERATED FILE —— 流程选择器索引（diagnose 方法缺口消费点读它），不要手改。
# 由 scripts/build_procedure_index.py 生成；--check 校验新鲜度 + 可解析性（CI）。
#
# 用法（形态归 SKILL、内容归词条）：
#   1) 按本轮 category 过滤 categories——**该列为空 = 不限定类别，照常进入候选**；
#   2) 用 title/summary 选**一条**最贴合的流程（本索引只是选择器，不是内容）；
#   3) 按 file 打开该词条读 **content.flow[] 全文**——摘要行不承载判据，只读摘要等于没加载；
#   4) 一轮诊断最多加载一条流程（成本有界）。
#
# 生成日期：2026-09-10    流程条数：17    本文件整体加载成本：约 2852 token（选择器每次都要整读，故单文件 + 记成本）
#
# 增长闸门（原则十一：按闸门不按日历）：流程条数 > 25，或本文件 > 5K token → 按 category 分片
#   （对齐 case 层 knowledge/_index/<ns>__<category>.yaml 的做法）。当前单文件即可。
entries:
- id: ascend-aicore-error-triage
  title: AI Core Error 问题定位（RAS → NPU 硬件 → 软件 → msaicerr 分析）
  summary: AI Core Error 按四层分流：①event 日志搜 event_id 查 RAS 硬件故障（npu-smi health 兜底）→ ②plog 搜 ECC/同 chipId 多次报错，ascend-dmi 压测核验 NPU 硬件 → ③索引类算子 0x800000 报错查软件越界 → ④msaicerr 输出分析报告；收集阶段框架业务免复跑、非框架业务复跑+设 NPU_COLLECT_…
  categories:
  - interrupt
  platforms:
  - cross
  file: references/methodologies/ascend-aicore-error-triage.yaml
- id: ascend-determinism-triage
  title: 确定性失效排查路径（开关被重置 / 算子内部竞争 / batch invariance 三路分流）
  summary: 训练或推理结果不可复现时的分流路径：先确认确定性开关是否**真的**开着（易被业务代码中途重置）→ 再看「全量 dump 是否让问题消失」（消失=采集行为影响时序，指向竞争）→ 三路分流：①plog 看 aclnn deterministic 标识是否中途变 0（业务代码重置开关）②ASCEND_LAUNCH_BLOCKING=1 后消失 + mssanitizer 报竞争（算子内部数据竞争）③…
  categories:
  - precision
  platforms:
  - cross
  file: references/methodologies/ascend-determinism-triage.yaml
- id: ascend-garbled-output-triage
  title: 乱码类「无报错错误输出」分流路径（权重错位 / 模块未移植 / 算子数值偏差 / KV 传输时序 / 传输路径未定位）
  summary: 推理或训练输出乱码且**无报错**时的分流路径：先按「三段定界」（输入预处理 / 前向计算 / 传输与后处理）判断问题落在哪一段，再按五条机制分流——①权重转换/加载后逐 tensor 错位 ②模型模块未移植（分层分界，如第 N 层后异常）③算子数值偏差（版本绑定，回退版本即消失）④KV 传输/流同步时序（首请求、跨 PP stage 边界）⑤传输路径缺陷未定位（切换通信路径可规避）；乱码没有签…
  categories:
  - precision
  platforms:
  - cross
  file: references/methodologies/ascend-garbled-output-triage.yaml
- id: ascend-log-troubleshooting
  title: 日志异常排查（不落盘 / 时间戳不对 / 多余打屏）
  summary: 日志故障按现象分流：不落盘按部署形态分支排查（EP/RC/Control CPU × 应用/系统：系统日志报错 → df -h 磁盘 → ldd 动态库 → ps 查 slogd → 重启日志进程）；时间戳不对是 AI 任务启动后改系统时区所致（重启任务或 timedatectl 改回）；多余打屏是日志模块与 slogd 建 Socket 失败降级打屏（fd 超限用 ulimit -n 调大）
  categories:
  - interrupt
  platforms:
  - cross
  file: references/methodologies/ascend-log-troubleshooting.yaml
- id: ascend-moe-comm-triage
  title: MoE 通信/路由算子故障排查（MoeDistributeDispatch / MoeInitRouting / QuantBatchMatMul）
  summary: MoE 相关算子（aclnnMoeDistributeDispatchV2/V4、MoeInitRoutingV3、QuantBatchMatMulV3）执行报错按形态分流：507057 SUSPECT REMOTE ERROR + MTE 越界 → ROCE 组网兼容性（升级 0.20.2rc1+ / HCCS 退避）；561000 Cannot find binary → torch-npu…
  categories:
  - interrupt
  platforms: []
  file: references/methodologies/ascend-moe-comm-triage.yaml
- id: ascend-mooncake-kv-transfer-triage
  title: Mooncake/Ascend KV-transfer 分层诊断（python backend / native transport / 连接 / 流同步）
  summary: PD 分离 + Mooncake KV-pool 部署故障先按报错面路由到层、再按层内形态定位，把同族诊断从逐个翻 case 变成分层路由：python traceback 且栈指 vllm_ascend/*.py → python backend 层（vllm-ascend 自身代码， 细分 init/线程 config、transfer-group 索引、DCP/MTP 切分长度、调度 KV…
  categories:
  - interrupt
  - precision
  platforms:
  - A2-910B
  - A3-910C
  file: references/methodologies/ascend-mooncake-kv-transfer-triage.yaml
- id: ascend-oom-triage
  title: 内存 OOM 问题定位（C 接口 → CANN Host 内存 → CANN Device 内存 → 业务进程异常）
  summary: OOM 按申请方分流：C 标准接口 malloc/memset 申请 Host 内存→asan 检测；CANN 接口 aclrtMallocHost 申请 Host 内存→日志搜 mem_stats 看组件统计；aclrtMalloc/aclrtMallocPhysical/hi_mpi_dvpp_malloc 申请 Device 内存→mem_stats；其它报错→搜 DEV_PROC_MEM…
  categories:
  - interrupt
  platforms:
  - cross
  file: references/methodologies/ascend-oom-triage.yaml
- id: ascend-process-hang-triage
  title: 进程卡住问题定位（调用栈定位下发/执行阶段 → 卡住任务 → 编译耗时）
  summary: 进程卡住先用 asys collect -r=stacktrace 取调用栈：栈无 CANN 内部函数→任务未下发查客户脚本；有 SendTask→卡在任务下发；有 Synchronize→卡在任务执行，用 trace 日志 stream_id+head/pos 定位卡住任务；plog 有 watchdog timeout 或算子停留 Compile process status/init di…
  categories:
  - interrupt
  platforms:
  - cross
  file: references/methodologies/ascend-process-hang-triage.yaml
- id: ascend-process-interrupt-triage
  title: 进程中断问题定位（首报错 → 接口错误 → 任务执行错误 → 心跳丢失 → 资源未释放）
  summary: 进程异常退出按五层排查：①plog 首报错有错误码（非 E*9***）查错误码参考；②acl***/rt*** 接口报错（对外接口查 API 参考，内部接口联系技术支持）；③fault kernel_name 算子执行报错 / Task run failed 内部任务报错（ascend-dmi 压测）；④Device lost heartbeat+HEARTBEAT EXCEPTION 或 fa…
  categories:
  - interrupt
  platforms:
  - cross
  file: references/methodologies/ascend-process-interrupt-triage.yaml
- id: ascend-train-precision-triage
  title: 大模型训练精度定位五步路径（CheckList → 复现前置 → 稳定/不稳定分流 → 分支排查 → 单算子确认与修复）
  summary: 训练侧精度问题定位五步路径：先做 CheckList 排除非算子因素（超参/环境变量/三方库/数据读取/模型结构/权重初始化/版本）→ 复现前置（固定随机性、打开确定性、seed_all、缩小规模）→ 按「能否稳定复现 + 现象」分流（溢出或NaN / 首Step Loss差异 / 长稳Loss差异 vs 内存踩踏/算子确定性/硬件/框架/版本）→ 分支排查 → 单算子三方比对确认后用「修复三板…
  categories:
  - precision
  platforms:
  - cross
  file: references/methodologies/ascend-train-precision-triage.yaml
- id: ascend-vllm-spec-decode-mtp-triage
  title: vllm-ascend spec-decode/MTP 族分层诊断（启动/首请求/运行期崩溃 + 静默精度·性能退化）
  summary: vllm-ascend 投机解码（spec-decode/MTP：deepseek_mtp/qwen3_5_mtp/qwen3_next_mtp/EAGLE/DSpark）故障族， 先按故障阶段分流——R-A 启动/拉起期（服务起不来、图捕获 warmup 崩）、R-B 服务能起但首请求/投机 first pass 即崩、R-C 运行期崩（压测到并发门槛或上线后）、R-D 无异常栈静默异常（阻塞…
  categories:
  - interrupt
  - precision
  - performance
  platforms:
  - A2-910B
  - A3-910C
  file: references/methodologies/ascend-vllm-spec-decode-mtp-triage.yaml
- id: glm-quantized-startup-triage
  title: GLM 系模型量化加载启动失败排查（按报错形态分类）
  summary: GLM 系模型（GLM-5/5.1/5.2）w8a8/w4a8/w4a8c8 量化权重拉起 vllm serve 启动失败时，先按报错形态分类定位——modelslim 描述缺失 / 版本错配 / 算子绑定 / 量化路由，各类修法不同
  categories:
  - interrupt
  platforms: []
  file: references/methodologies/glm-quantized-startup-triage.yaml
- id: msllm-startup-version-env-triage
  title: mindspeed-llm 训练启动失败排查（版本/环境配套面 + import 失败判别）
  summary: mindspeed-llm 训练启动即失败的常见根因分流：import 失败先判别"版本/安装面 vs 代码漂移面"（features_manager 缺符号=版本不配套；tasks.checkpoint.models 缺失=上游删模块未同步后端）；算子重复注册（CANN 版本过旧 vs 多版本 mindspeed）；特性未适配（batch_p2p_comm 等需升级 PTA 或关闭）。按报错签…
  categories:
  - interrupt
  platforms: []
  file: references/methodologies/msllm-startup-version-env-triage.yaml
- id: msprof-comm-bottleneck-thresholds
  title: 通信瓶颈判定阈值（Wait Time Ratio / 传输前同步比例 / 带宽使用率）
  summary: msprof 官方的通信瓶颈判定口径：①一个迭代内是否存在「等待时长比例（Wait Time Ratio）> 0.2」的卡——有则通信瓶颈；②找 WTR 最大的卡看「传输前同步时长比例 > 0.2」——大于则存在慢卡（查慢卡前反向计算时间与数据预处理），否则链路异常；③通信矩阵：带宽使用率 > 0.8 说明链路正常、通信包比例 > 0.8 指向链路配置/劣化、包尺寸过小指向带宽瓶颈
  categories:
  - performance
  platforms:
  - cross
  file: references/methodologies/msprof-comm-bottleneck-thresholds.yaml
- id: rl-train-infer-consistency-triage
  title: RL 训推一致性排查路径（先定分歧面，再选权重链路 / 概率偏移 / 阶段定界）
  summary: RL（GRPO/PPO）训推一致性问题的排查路径：先按现象定分歧面——输出崩溃（乱码/生僻字/重复）多指向权重同步链路，reward 下降 + logp_diff 上升多指向逐 token 概率偏移，整体效果差但难分训推则需阶段定界；再选手段（UWC 三段比对权重链路 / 逐 token probs_diff 看概率分布 / 交叉打桩定界训练还是推理），最后按各自容差判读
  categories:
  - precision
  platforms:
  - cross
  file: references/methodologies/rl-train-infer-consistency-triage.yaml
- id: vllm-ascend-startup-triage
  title: vllm-ascend 服务启动失败通用排查（跨模型：版本配套 / 量化加载 / patch 层 / 平台特化路径）
  summary: 通用 vllm-ascend（vllm serve / EngineCore）启动即失败的分流方法论——先按失败阶段与报错签名分类，依次核对版本/配套面（vllm×vllm-ascend commit、上游支持合入点、适配期坏版本、镜像内依赖）、量化权重格式面、patch 层全局副作用，再定位平台特化执行路径缺陷（启动期图捕获 / MoE 路由与 EP / 特殊 loader），最后查部署与环境…
  categories:
  - interrupt
  platforms: []
  file: references/methodologies/vllm-ascend-startup-triage.yaml
- id: vllm-infer-precision-triage
  title: vLLM 推理精度定位五步路径（前置对齐 → 首差异 token → dump → compare → 单算子确认）
  summary: vLLM 推理精度定位五步路径（单 case 可复现场景）：问题场景与标杆场景对齐（配置/超参/三方库/输入）并固定采样随机性（temperature=0）与算子确定性 → 打印输出 token_id 序列定位**首个差异 token**（把"整句不对"缩到"第 N 个 token 起不对"）→ 两侧使能 msprobe dump（推荐 mix+statistics）→ msprobe comp…
  categories:
  - precision
  platforms:
  - cross
  file: references/methodologies/vllm-infer-precision-triage.yaml
