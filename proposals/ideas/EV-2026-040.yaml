# ============================================================
# Idea 卡：流程类先验知识的加载机制（procedure 闸门 + 选择器索引 + 观测）
# 依据：三轮盲测（12 + 9 + 21 次）与 EV-2026-038 的判定——内容留 reference、结构触发、**必须加载全文**。
# 本卡补的是"让判定生效"的执行面：在 EV-2026-038 里登记为"实现动作（待批准）"的三项 + 索引语义拆分。
# ============================================================

id: EV-2026-040
layer: L2
title: "流程加载机制：procedure 闸门 + 流程选择器索引（methodology 退出背景 summary 层）+ procedure 观测字段"
status: validated
authorization: review
dimension: process
supersedes: []
superseded_by: null
created_at: 2026-09-10
source_signals:
  - signal: process_friction
    evidence: >-
      EV-2026-038 的判定（内容留 reference / 结构触发 / 必须全文）此前只落在决策卡里，
      仓库中**没有任何代码让流程真的被加载**：12 条真实 trace 里精度/性能会话加载流程 0 次；
      `collect-gates.yaml` 只有数据缺口（probe/conditional）两类闸门，没有方法缺口消费点
    trajectory:
      - "proposals/ideas/EV-2026-038.yaml（判定与三轮实验）"
      - "traces/*.yaml（12 条，purpose 全为 signature）"
      - "skills/diagnose/references/collect-gates.yaml（两类闸门，无 procedure）"
  - signal: process_friction
    evidence: >-
      `_summary-index.yaml` 双重语义：methodology 行既当"背景提示"（2.5 ②）又必须当"流程选择器"（新机制），
      同一文件两种语义靠 type 隐式分流——属自评中已指出的腐化点；且实测摘要行当内容用与不加载等效
    trajectory:
      - "scripts/build_ref_summary_index.py（BG_TYPES 含 methodology）"
      - "skills/diagnose/references/diagnosis-procedure.md:66,72（行内 summary 即背景提示，不读全文）"
hypothesis: >-
  在 diagnose 的「方法缺口」处补一个结构化消费点（kind: procedure 闸门 → 流程选择器索引 →
  **读 content.flow[] 全文**），并把 methodology 从背景 summary 层拆到独立选择器索引，
  即可让流程进入诊断上下文且可归因；观测面（purpose=procedure + procedure_follow 事件）是它的验收前提
predicted_effect:
  metric: 精度/性能会话的流程加载率 + 流程跟随可归因性
  from: "precision/performance 会话加载 0 次；purpose 词表无 procedure；无 procedure_follow 事件"
  to: "procedure 闸门命中即加载全文（可测）；trace 可记 procedure 全文加载 + 跟随深度/分支/缺口"
validation:
  method: scan_review
  baseline: "三轮盲测结论 + 现有 collect-gates/verify_references 校验路径"
  success_criteria: >-
    ①`verify_references.py` 对 kind=procedure 强校验（trigger + load: full + selector 存在），
    负例逐条验红；②methodology 从 `_summary-index.yaml` 消失、出现在 `_procedure-index.yaml`，
    两索引 --check 均绿；③KB-checks CI 增加 `build_procedure_index.py --check`；
    ④diagnose SKILL/procedure/trace 三处口径一致（全文加载 + gap 如实 + procedure 词表）；
    ⑤`trace_metrics.py` 词表同步（procedure purpose + procedure_follow action）
  rollback: 分支合入前可整体丢弃；合入后 git revert（原则七）
gate:
  condition: "用户 2026-09-10 指示『把缺的补上』（EV-2026-038 的四个未落地项）"
risk: medium
principle_refs: [2, 4, 8, 9]
actual_cost:
  tokens: 60000
  source: estimate
decisions:
  - who: agent（机制实现轮）
    when: "2026-09-10"
    type: proposal
    conclusion: >-
      四项缺口：①procedure 闸门（加载机制本体）；②观测字段（purpose/action/指标）；
      ③流程留出检验接进 groom；④`_summary-index.yaml` 双重语义拆分。
      设计取舍：**闸门不写症状关键词分支**——症状→流程的选择由词条自身的 title/summary 经选择器索引承担
      （关键词写进闸门 = 与 triage-tree 双源，必漂移；且第三轮实测"只给索引让 agent 自己选"7/7 选对，
      无需预置关键词）。这是相对 PLAN.md §3.1 初稿的修正。
  - who: agent（机制实现轮）
    when: "2026-09-10"
    type: action
    conclusion: >-
      ①`scripts/build_procedure_index.py`（新）+ `references/_procedure-index.yaml`（17 条流程，含用法四条）；
      ②`build_ref_summary_index.py` 的 BG_TYPES 去掉 methodology（71 → 54 条背景类）；
      ③`skills/diagnose/references/procedure-gates.yaml`（三个 category 的 kind=procedure 闸门，
      load: full + selector + trigger + caveat_refs）；④`verify_references.py`：VALID_GATE_KINDS 增 procedure
      + 专属性校验分支；⑤diagnose SKILL「方法缺口」段 + procedure.md 步骤 2.5 ④（全文加载/一条流程/gap 如实）
      + trace.md 的 procedure_follow 字段；⑥`trace_metrics.py` 词表同步；⑦groom R10 流程留出检验
      （`flow_pool.py --flow-coverage`）；⑧`docs/metrics.md` 增"流程加载与跟随"指标行；⑨kb-checks 增
      procedure 索引新鲜度 step 与 paths；⑩CONTEXT.md / references/README.md 同步。
  - who: agent（机制实现轮）
    when: "2026-09-10"
    type: eval
    conclusion: >-
      scan_review：`verify_references.py --check` / `build_ref_summary_index.py --check` /
      `build_procedure_index.py --check` / `build_index.py --check` / `verify_metrics.py --check` /
      `verify_proposals.py --check` / skill 自包含 grep 全绿。**未跑检索 golden**——本卡不改候选路由/排序
      （流程不参与 Tier 1/2 检索），改动面为"新增消费点 + 索引拆分 + 观测词表"，按 docs/eval.md 门禁分级
      属"交互/指引面"，扫查 + 人审覆盖；真实效果由 procedure 加载率指标在启用后验证（先落观测，不预判）。
  - who: agent（机制实现轮）
    when: "2026-09-10"
    type: decision
    conclusion: >-
      采纳（validated）：随 PR 送人审（review——L2 流程改动，进诊断上下文的是"什么时候读什么"而非结论）。
      不取：①在闸门里写症状关键词分支（与 triage-tree 双源，必漂移）；②把流程做成 skill（第一轮已证形态无关，
      且会丢 schema/来源/CI 绑定校验）；③摘要行 + 按需读全文（第二轮已证摘要当内容与不加载等效）；
      ④一条诊断加载多条流程（成本无界，规则定为"最多一条，必要时按 related_references 追加"）。
      遗留：#165（归档 3 条案例索引型）与本 PR 都重建 `references/_summary-index.yaml`，
      **谁后合谁要 rebase 并重建两个索引**（机械动作）；`eval/flow/README.md` 的观测字段说明在 #166，
      合并后可按本卡口径补一行。
  - who: agent（提交前自评轮）
    when: "2026-09-10"
    type: action
    conclusion: >-
      自评发现并修掉四个隐性问题（均在合入前）：①**生成物不是合法 YAML**——`render()` 逐行拼接 +
      `safe_dump` 单标量在默认 width=80 处折行顶掉缩进，而 `--check` 只比文本（两次同样错误的生成结果），
      属**自证式校验**，CI 全绿也发现不了；已改为整篇 safe_dump + `--check` 增"产物必须可解析"断言。
      ②**加载时机自相矛盾**：SKILL 把流程写在「数据资产探询」节（=候选加载前），procedure.md 写在 2.5
      （=候选之后）；且"候选前加载"会让流程强先验在评估候选前**锚定**解释框架（第三轮实测该分支会误导）。
      已统一到**步骤 5（候选全部未命中后）**，三个闸门 trigger 同步改写。
      **此择时是设计判断、非实验结论**：三轮实验都没有"候选集在场"的场景，无法直接对照（第三轮只有
      "流程先于/后于日志"的顺序臂，不构成候选在场时的对照）。判据是推理——候选命中时流程用不上，
      而候选前加载会锚定解释框架（第三轮已实测该分支误导）。可证伪的观测：合入后若出现"候选明确命中
      却仍加载流程"或"候选全未命中却从不加载流程"，即择时错，回看本条并重定。
      ③**一轮一条的边界没写死**：补"该流程走完仍未定位 → 转深度排查/人工，不换第二条"（对齐连续失败 ≤2）。
      ④**错流程无反馈回路**：补 `attribution` 事件的 `component: reference:<ref-id>` 口径
      （跟随流程给出 fix 但没解决时写），让 `component_tally.py` 能聚合"被跟随后仍失败"的流程簇——
      否则流程层只有加载率（活动度量）没有失败率，错流程会被稳定注入而无人察觉。
      另补：流程索引的增长闸门（条数 > 25 或 > 5K token → 按 category 分片）与实测成本（17 条 = 2.9K token，
      选择器每次整读）；groom R10 容忍 `flow_pool.py` 缺席（脚本随 #166 引入，避免跨 PR 悬挂指令）。
  - who: agent（合并 + 自评轮 2）
    when: "2026-09-10"
    type: decision
    conclusion: >-
      **四个 PR（#165/#166/#167/#168）合并为一个**（同一目标问题：流程类先验知识的载体与加载），
      合并时仅 `references/_summary-index.yaml` 冲突（两处重建）→ 重新生成解决；流程索引随之 17 → 14。
      **给模型留出判断空间的调整（两处）**：①流程从"一轮只许一条"改为"**默认一条**，前提与现场证据
      明确矛盾时可换一条"（同样受"连续失败 ≤2"约束）——原来的硬限制比 case 层的规则更严，属过度约束；
      ②新增 `procedure_follow.conflict` 字段并写明"**流程与证据冲突时以证据为准**，流程是参考不是判词"，
      记冲突不是记叛逆（流程被判据推翻是正常结果，写下来才能让流程层知道自己哪步在这现场不成立）。
      **强度标注（原则十）**：`docs/metrics.md` 补记"跟随深度是 agent 自报的弱观测，不可当验收证据；
      只有加载率是确定性的、跟随后 resolve 率是较强信号"——这是自评发现的漏标（#8）。
  - who: agent（第四轮实验：加载时机对照 + 选择器泛化）
    when: "2026-09-10"
    type: decision
    conclusion: >-
      **加载时机的对照做完了，结论推翻了本卡原来的论据**（预登记见实验记录 SCORE-4，54 格盲测）：
      ①H 组（3 个 fixture 自带 `candidates=Y`）× C 臂（步骤 5，6 格）vs P/P2 臂（流程先行，11 格）——
      C 臂 **0/6** 加载流程（符合口径），P/P2 臂 **11/11** 加载并执行，**但无一例放弃或偏离命中 case**
      ⇒ 规则 R1（锚定危害）不成立、R2（无差异）成立。②本卡原写的"第三轮实测：流程的分支判别把 agent
      推入过高置信错误分支"**归因错了**——那两例（009/010）的样本**不在**该流程收录范围内，
      误导来自**流程不匹配**（内容面）而非**加载时机**（顺序面）。③故时机选择的理由改为**预算**（原则九）：
      候选命中时流程用不上，提前加载只是白付注意力预算——这是成本论断，强度如实标"设计判断"，
      **不得再当实测结论引用**；SKILL 与 procedure-gates.yaml 的措辞已同步改写。
      ④触发点确实补上了：改用两个**尚未沉淀 case** 的新 issue（14378/12102）重测，C 臂 **3/4 走到步骤 5
      并加载流程全文**（对照改动前 12 条真实 trace 加载 0 次）；且 12102 一格正确执行了"流程与证据冲突
      → 以证据为准、不换第二条、记 conflict"，0 例硬凑——`procedure_follow.conflict` 字段实测可用。
      ⑤选择器泛化关闭：近邻对（glm-quantized ↔ msllm 启动失败，两条 title 都含"启动失败排查"）
      6 样本 + 4 次"应弃用"负对照 = **16/16 全对**（第三轮 7/7 的扩展；含难样本 11208——
      报错是 msllm 的招牌症状 `ModuleNotFoundError` 但部署面是 GLM/vllm，两格都选对）。
      ⑥本轮暴露两条须随 PR 修掉的：**SKILL 里两个 `references/` 同名不同目录**（skill 支撑文件 vs 仓库根
      先验层在「方法缺口」一节并排出现，已有 agent 据此断言 `procedure-gates.yaml` 不存在）；
      **回放记录会腐烂**（见下条 decision 与 `eval/flow/README.md` 使用纪律）。
  - who: agent（第四轮实验：评测完整性）
    when: "2026-09-10"
    type: decision
    conclusion: >-
      **"候选未命中"是会被自己消耗掉的状态**：回放一条 miss → 诊断成功后沉淀出 case →
      同一 issue 下次变 hit。日期实证：`.s2-replay` 回放日 2026-09-03，而 `VLLM-ASC-10097` 的 case
      提交日 **2026-09-03**、`13639`/`12461` **2026-09-09**、`13973`/`12989` **2026-09-02**；
      本打算用作"候选未命中"组的 6 个样本因此 **12/12 全部命中**（C 臂加载流程 0/6，前提失效），
      改用两个尚未沉淀 case 的新 issue 才测出真实触发率（3/4）。
      处置：`eval/flow/README.md` 增「使用纪律」一节——golden fixture 头部的 `candidates=Y/N` 与
      `eval/s2` 的 `tier2_hit` **都是历史记录、不是当前状态**；任何以"候选未命中"为前提的实验必须
      **当场重跑或改用尚无 case 的新 issue**；并且 **miss 率下降既可能来自检索变好、也可能来自库变大**，
      不可单看趋势下结论。此条不改评测集本身（样本仍有价值），改的是**读它的方式**。
