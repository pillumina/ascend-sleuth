# eval/golden/MSLLM-1655.fixture.yaml
# 真实夹具：源自 MindSpeed-LLM issue #1655（公开 issue；输入 = 用户首帖原文，剥掉平台自动附的安全提醒样板与页脚，
# 唯一删改是把模型 config.json 的附件链接（含上传 id 与哈希）换成占位）。
# 训练侧夹具（interrupt 面）：与上一条同一侧、不同性质，故两条一起才盖住训练侧的性质判别。
# 症状里同时有 "if __name__" 之外的宽词（RuntimeError）与栈尾签名，测的是"宽词之下仍落到 interrupt"。
# 本条回放结果：candidates=Y rank=1 path=primary

case_id: MSLLM-1655

input:
  symptoms: |
    ### 环境信息

    ```
    例如：
    - 操作系统
    - 昇腾硬件信息
    - CANN软件版本
    - 安装的对应软件版本
    ```
    Ubuntu 22.04 LTS
    Ascend950DT
    25.1.rc1.b162
    26.1.0

    ### 🐛 问题描述

    预训练gemma-2-9b模型时，框架无法识别architectures：Gemma2ForCausalLM报错：
    When context_parallel is not activated, kv_head_repeat_before_uly_alltoall would be set to False for reducing memory usage.
    Traceback (most recent call last):
      File "/MindSpeed-LLM/pretrain_gpt.py", line 309, in <module>
        main()
      File "/MindSpeed-LLM/mindspeed_llm/training/utils.py", line 1136, in wrapper
        return func(*args, **kwargs)
      File "/MindSpeed-LLM/pretrain_gpt.py", line 302, in main
        pretrain(train_valid_test_datasets_provider,
      File "/MindSpeed-LLM/mindspeed_llm/training/training.py", line 467, in pretrain
        initialize_megatron(extra_args_provider=extra_args_provider,
      File "/MindSpeed-LLM/mindspeed_llm/training/initialize.py", line 191, in wrapper
        result = fn(*args, **kwargs)
      File "/MindSpeed-LLM/megatron/training/initialize.py", line 86, in initialize_megatron
        validate_args(args, args_defaults)
      File "/MindSpeed-LLM/mindspeed_llm/training/arguments.py", line 216, in wrapper
        MindSpeedFeaturesManager.validate_features_args(args=args)
      File "/MindSpeed/mindspeed/features_manager/features_manager.py", line 63, in validate_features_args
        feature.validate_args(args)
      File "/MindSpeed-LLM/mindspeed_llm/features_manager/convert_checkpoint/convert_checkpoint.py", line 158, in validate_args
        args.model_type_hf = infer_model_type_from_hf_config(config_path, MODEL_TYPE_HF_CHOICES)
      File "/MindSpeed-LLM/mindspeed_llm/training/utils.py", line 1122, in infer_model_type_from_hf_config
        raise RuntimeError(
    RuntimeError: Cannot infer model type from architectures of Huggingface config.json '{arch_row}'. Please specify --model-type-hf explicitly.
  framework: mindspeed-llm
  platform: A5-950

expected:
  namespace: training/mindspeed-llm/interrupt/
  case_id: MSLLM-1655
  assertion: top-3   # LLM 非确定性口径（docs/eval.md）
